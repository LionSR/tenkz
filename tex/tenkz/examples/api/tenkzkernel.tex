\documentclass{standalone}
\usepackage{tenkz}
\begin{document}
% The package binds the kernel surface at load; the call below rebinds the
% same meanings, so an opt-in document keeps compiling unchanged.
\tenkzkernel
\begin{tenkz}[rows={wire}, cols=1, bonds=none]
  \tnwire[weight=bundle=3]{w outside}{e outside}
\end{tenkz}
\mbox{kernel spelling parsed}
\end{document}
