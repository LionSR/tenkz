\documentclass{standalone}
\usepackage{tenkz}

\begin{document}
% For each row r, east=cup contracts the ket and bra virtual boundary
% indices by delta_{v^ket_{E,r},v^bra_{E,r}}.  The six internal vertical
% segments contract the on-site physical ket-bra pairs.  The two west
% endpoints on each row and the two north and south endpoints on each
% column remain free; outer legs=none suppresses only the physical faces.
\begin{tenkzlattice}[
    rows=3, cols=2, sheets={ket,bra}, boundary=open,
    outer legs=none, east=cup]
  % The environment body stays available for local presentation-layer
  % customization.  These records decorate one ket site and bond without
  % changing the cup contraction or any boundary ownership.
  \tnsite[role=marked, label=$A_\star$]{(2,2,1)}
  \tnedge[role=marked]{(2,1,1)-(2,2,1)}
\end{tenkzlattice}
\end{document}
