# What the tenkz upload archive contains, pinned.
#
# `scripts/tenkz_ctan.py` walks the load graph from `tex/tenkz/tenkz.sty` and
# compares the walk against `[runtime]` below. The pin is not the source of
# the closure — the entry point is — so this file cannot make the archive
# carry a stage the package does not load, or hide one it does. What the pin
# buys is a failed check when the load graph moves: a new stage module, a new
# required package, a new TikZ library, or a deleted one, all stop here until
# somebody decides whether the upload changes with them.
#
# The staged order is the load order for the runtime files and the order below
# for the rest. Member order is part of the archive's bytes.

schema = 1
package = "tenkz"

[runtime]
# Walked from `tenkz.sty`, in load order. The last two are the lockout: the
# render stage creates the ink primitives, so it loads after every stage that
# might reference them, and the kernel language loads after render so its
# records meet the same primitives.
files = [
  "tenkz.sty",
  "tenkz-language.code.tex",
  "tenkz-language-registry.tex",
  "tenkz-model.code.tex",
  "tenkz-core.code.tex",
  "tenkz-metric.code.tex",
  "tenkz-geometry.code.tex",
  "tenkz-string.code.tex",
  "tenkz-tree.code.tex",
  "tenkz-render.code.tex",
  "tenkz-kernel.code.tex",
  "tenkz-kernel-language.code.tex",
  "tenkz-kernel-policy.code.tex",
  "tenkz-kernel-resolve.code.tex",
  "tenkz-kernel-ink.code.tex",
  "tenkz-kernel-annotate.code.tex",
  "tenkz-kernel-surface.code.tex",
]

[runtime.requires]
# What the archive does not carry and the installation must already have.
# Three of the libraries come from packages of their own rather than from
# pgf: `hobby` and `spath3` are separate CTAN packages, and an installation
# without them fails at load, not at the first curve.
packages = ["tikz"]
libraries = [
  "arrows.meta",
  "backgrounds",
  "calc",
  "decorations.markings",
  "decorations.pathreplacing",
  "fit",
  "hobby",
  "intersections",
  "shapes.geometric",
  "spath3",
]

[runtime.requires.ownership]
# Why each library is loaded, traced to the code that reads it.
# `docs/tenkz/ctan/DEPENDENCIES.md` is the reading; this is the pin, and the
# check refuses a library that enters or leaves the load list without one.
#
# `placement` is what the package computes and places with: take one away and
# the model has no answer for where a tensor, a wire, or a crossing goes.
# `ink` is what the drawing needs: take one away and the network is placed and
# undrawable, an arrow tip or a brace or a station outline missing. The
# distinction matters to an author, because the second group is what a host
# document's own styles reach into and the first is not.
placement = ["calc", "hobby", "intersections", "spath3"]
ink = [
  "arrows.meta",
  "backgrounds",
  "decorations.markings",
  "decorations.pathreplacing",
  "shapes.geometric",
]
# Loaded, and read by nothing. A library is reached through the names it
# defines rather than through its own, so this class is not settled by looking
# for the library's name: it is settled by removing the load and compiling the
# corpus, which is the reading DEPENDENCIES.md records. `fit` comes from pgf
# rather than from a package of its own, so carrying it costs an installation
# nothing beyond `tikz`, which is why it is recorded here instead of dropped:
# removing a load is a change to what a host document inherits at package load,
# and that is a release decision rather than a staging one.
unconsumed = ["fit"]

# Documentation sources are staged under doc/ from MANUAL_SOURCES in
# scripts/tenkz_manual_build.py, the same inventory the isolated build uses.
# They are not repeated here; check rebuilds the manual from the upload.
[material]
# Staged name = the file it is copied from, unmodified. The version and date
# these carry are checked against the `\ProvidesPackage` line; none of them
# declares a version of its own.
"README.md" = "docs/tenkz/ctan/README.md"
"LICENSE" = "LICENSE"
"CHANGES.md" = "docs/tenkz/CHANGES.md"
"CITATION.cff" = "docs/tenkz/ctan/CITATION.cff"
"tenkz.bib" = "docs/tenkz/ctan/tenkz.bib"
"tenkz.pdf" = "output/pdf/tenkz-manual.pdf"

[source_tree]
# Everything in `tex/tenkz/` the upload deliberately leaves out. Anything else
# found there that the entry point does not load fails the check.
#
# `examples/` holds the registry's one-picture-per-command examples. They are
# compiled by the manual doc-test and are not reader documentation.  The
# upload carries the reproducibly built manual as `tenkz.pdf` instead.
excluded = ["examples"]
