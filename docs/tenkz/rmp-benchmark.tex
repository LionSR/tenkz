% rmp-benchmark.tex -- generated-index driver for the tenkz RMP benchmark.
%
% Compile through scripts/tenkz_rmp.py.  The driver defines
% \TenkzRMPGeneratedIndex to a temporary file assembled from manifest.toml;
% this document never contains or copies a benchmark case body.
\documentclass[10pt,twoside]{article}
\usepackage{tenkzrmpbenchmark}
\usepackage[colorlinks=true,linkcolor=RMPAccent,urlcolor=RMPAccent]{hyperref}
\hypersetup{
  pdftitle={The tenkz RMP benchmark book},
  pdfauthor={The TNLean project}
}

\providecommand\TenkzRMPGeneratedIndex{rmp-benchmark-index.tex}

\begin{document}
\begin{titlepage}
  \centering
  \vspace*{28mm}
  {\Huge\bfseries The tenkz RMP benchmark book\par}
  \vspace{5mm}
  {\Large A source-first laboratory record\par}
  \vspace{16mm}
  \begin{minipage}{.66\textwidth}
    A benchmark target has one canonical, preamble-free source file.  The
    checker wraps that file temporarily to produce the vector rendering
    embedded here; this book reads the original for the line-numbered listing.
    The manifest supplies order and provenance, never a second copy of the
    body.
  \end{minipage}
  \vfill
  {\small TNLean \textbullet{} generated by \texttt{scripts/tenkz\_rmp.sh}\par}
\end{titlepage}

\tableofcontents
\clearpage
\input{\TenkzRMPGeneratedIndex}
\end{document}
