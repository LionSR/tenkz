% One-page environment chooser.
\section{Which environment?}\label{ch:chooser}

When you sit down to typeset a tensor network, your very first design
decision is to select the right environment.  A simple yet profound principle
will guide you: \textbf{choose from topology, not appearance.}

\medskip
{\small
\noindent
\begin{tabularx}{\linewidth}{@{}
  >{\raggedright\arraybackslash}p{24mm}
  >{\raggedright\arraybackslash}p{39mm}
  >{\raggedright\arraybackslash}X@{}}
\toprule
Layout & Use it when & Recognition test \\
\midrule
\tnenv{tenkz} & The picture is a regular one-dimensional contraction. &
Rows are layers, columns are sites, and neighbouring cells contract. \\
\bottomrule
\end{tabularx}
}
\medskip

In \pkg{}, the overwhelming majority of regular diagrams you will ever draw
begin with \tnenv{tenkz}.  A spatial lattice, a projected PEPS sheet, a circuit
window, or a measured region is not a separate, ad-hoc layout: it is simply a
\tnenv{tenkz} picture equipped with an appropriate frame---such as the
convenient \tnkey{lattice=} or \tnkey{planes} sugar keys, or a declared
geometric basis.

\paragraph{Resist changing layouts for style.}
It is often tempting to switch environments whenever a diagram looks slightly
different, but remember that the layout establishes the shared metric, atom
skins, semantic roles, labels, and connection policies across every picture in
your paper.  A one-dimensional chain does not cease to be a chain merely because
it runs vertically on the page!  Choose the layout that reflects the topological
structure of the network, and then declare your stylistic and geometric
preferences through its public keys.

\paragraph{When none fits.}
What should you do if you encounter a diagram that seems to defy the standard
grammar?  Before reaching for raw TikZ drawing commands or inventing ad-hoc
syntax, pause to identify the missing mathematical class.  In the \pkg{} design
philosophy, a new key requires at least three independent, manifested consumers
in the literature, while a new command requires a genuinely new grammatical
class.  Until an extension clears that gate, treat the need as a capability
request, rather than burying uncanonical drawing hacks inside your document.
