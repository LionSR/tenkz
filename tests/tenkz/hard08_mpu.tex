% Regression: hard08_mpu — renders physical legs = a two-site unitary U (pill) applied to a single blocked A.
% Formula: physical legs = a two-site unitary U (pill) applied to a single blocked A.
% T08 — RMP AAeqUA (MPU figure): two MPU tensors A A on a wire with up+down
% physical legs = a two-site unitary U (pill) applied to a single blocked A.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) grid attempt, LHS is fine; the RHS needs the pill's TWO legs to land on
the wide box below (one leg per spanned column):
\[
  \begin{tenkz}[physical=updown, tensor style=box]
    \tn{A} & \tn{A}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={op,op}, tensor style=box]
    \tn[pill, wide=2, no legs]{U} & \\
    \tn[wide=2]{A} &
  \end{tenkz}
\]

(b) same RHS without `no legs` to show the pairing behaviour for wide cells:
\[
  \begin{tenkz}[rows={op,op}, tensor style=box]
    \tn[pill, wide=2]{U} & \\
    \tn[wide=2]{A} &
  \end{tenkz}
\]
\end{document}
