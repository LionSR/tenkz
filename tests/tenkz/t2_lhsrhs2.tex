% Regression: t2_lhsrhs2 — imported current-behavior fixture from the handoff corpus.
% t2_lhsrhs2 — RMP arXiv:2011.12127, parent-Hamiltonian intersection
% argument, second displayed step, files fig4_fig4-pepe_LHS2.pdf /
% RHS2.pdf: the step-1 constraint with the injective interior tensor
% inverted away.
%
% Formula:  applying the interior inverse to both sides of
%   sum A^{i_1} B^{i_2} L(i_3) |i>  =  sum B^{i_2} C^{i_3} R(i_1) |i>
% removes the inverted tensor and EXPOSES its indices: on the left the
% word keeps A at site 1 with holes east of it; on the right the word
% keeps C at the last site with holes west of it.  L and R remain the
% wide 3-leg boundary tensors closing the virtual loop.
%
% Declarative reading per side (rows={ket:nopair, bra}):
%   * \tn{A} / \tn{C}      — the surviving word tensor, physical leg up;
%   * \tnskip              — a hole: the removed site's column keeps its
%                            virtual wires open on both sides and its
%                            physical index open;
%   * \tnghost             — the bare wire across the site the term does
%                            not cover;
%   * wide pill in the bra row = the boundary tensor; west=cup +
%     east=cup tie the word's virtual ends into it.
%
% Noted gaps (routing, as recorded for this genre in t2_lhsrhs1/5):
%   * the reference turns a hole's open virtual ends UP into vertical
%     stubs; \tnskip leaves them horizontal at wire height;
%   * the reference routes the boundary tensor's physical leg up OVER a
%     cup bend; a cup cannot carry an open stub, so that leg is emitted
%     down from the bra-typed boundary row.
% As rendered the hole emits NO physical stub (only its virtual ends),
% which matches the reference: the inverse consumed that index.  The
% signatures balance across `=`: (vw 0, ve 0, pu 1, pd 1) each side.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: A [hole] (wire) over the boundary tensor L
  \begin{tenkz}[rows={ket:nopair, bra}, west=cup, east=cup,
                tensor style=box]
    \tn{A} & \tnskip & \tnghost{}\\
    \tn[pill, wide=3]{L} & &
  \end{tenkz}
  \;=\;
  % RHS: (wire) [hole] C over the boundary tensor R
  \begin{tenkz}[rows={ket:nopair, bra}, west=cup, east=cup,
                tensor style=box]
    \tnghost{} & \tnskip & \tn{C}\\
    \tn[pill, wide=3]{R} & &
  \end{tenkz}
\]
\end{document}
