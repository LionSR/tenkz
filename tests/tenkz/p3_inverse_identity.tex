% Regression: p3_inverse_identity — imported current-behavior fixture from the handoff corpus.
% Formula: (a)  A_i^{-1} A_i = id (x) id      (physical index contracted;
% p3_inverse_identity — arXiv:1804.04964, lines 206-248: the one-sided
% inverse of an injective tensor, and the blocked two-site version.
%
%   (a)  A_i^{-1} A_i = id (x) id      (physical index contracted;
%        the identity drawn as two facing wire brackets)
%   (b)  (A_1^{-1} A_1)(A_2^{-1} A_2) = bracket, loop, bracket
%                                     = D . (bracket pair)
%        where D = dim of the bond (1,2); the closed loop is Tr(id) = D.
%
% tenkz spelling: the sandwich pairs each column's physical index by a
% vertical wire; each identity bracket is a bare cup over rows={wire,wire}
% (east=cup = bracket opening west, west=cup = bracket opening east);
% the closed loop is west=cup + east=cup on the same ghost pair.
\documentclass[border=6pt,varwidth]{standalone}
\usepackage{tenkz}
\begin{document}
% (a)  A_i^{-1} A_i = id: sandwich with paired physical index equals
%      the bracket pair (identity on the virtual index pair)
\[
  \begin{tenkz}[sandwich]
    \tn{A_i^{-1}} \\
    \tn{A_i}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={wire,wire}, east=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \;
  \begin{tenkz}[rows={wire,wire}, west=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \ .
\]
% (b)  two blocked sites: the middle loop is Tr(id_{D}) = D
\[
  \begin{tenkz}[sandwich]
    \tn{A_1^{-1}} & \tn{A_2^{-1}} \\
    \tn{A_1}      & \tn{A_2}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={wire,wire}, east=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \;
  \begin{tenkz}[rows={wire,wire}, west=cup, east=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \;
  \begin{tenkz}[rows={wire,wire}, west=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \;=\; D\cdot\;
  \begin{tenkz}[rows={wire,wire}, east=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \;
  \begin{tenkz}[rows={wire,wire}, west=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \ .
\]
\end{document}
