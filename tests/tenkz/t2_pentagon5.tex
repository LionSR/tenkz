% Regression: t2_pentagon5 — renders ^3F ^3F = ^4F ^3F ^3F — re-association of a two-vertex.
% Target: RMP 2011.12127 fig3_pentagon5.pdf (pentagon-equation component).
% Formula:  ^3F ^3F = ^4F ^3F ^3F  —  re-association of a two-vertex
% splitting tree with three outgoing legs and one incoming root:
%     ( a ( b c ) )  ~=  ( ( a b ) c )
% (the paper draws root below with an incoming arrow, three leaves above
% with outgoing arrows, gray disc vertices).
% House idiom: \tntree — leaves on the top line, junction beads,
% a short rooted leg below, which is exactly the paper's orientation.
% Honest gaps: tree edges carry no direction barbs (the paper directs
% every leg away from the root, the root leg toward it), and the
% junctions render as house ink beads, not gray discs.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

% left: (b c) fuse first, then with a  |  right: (a b) first, then with c
\[
  \tntree{({}\,({}\,{}))} \;\simeq\; \tntree{(({}\,{})\,{})}
\]
\[
  {}^{3}F\,{}^{3}F \;=\; {}^{4}F\,{}^{3}F\,{}^{3}F
\]

\end{document}
