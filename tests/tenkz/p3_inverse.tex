% Regression: p3_inverse — renders A_i^{-1} A_i = id (x) id and the pairwise contraction of two inverse sandwiches.
% Formula: (first):  A_i^{-1} A_i = id (x) id   (contraction over the physical
% p3_inverse — arXiv:1804.04964, Sec. "Injective MPS", source lines 205-250:
% the one-sided inverse of an injective tensor, and the pairwise contraction
% of two inverse sandwiches.
%
% Formula (first):  A_i^{-1} A_i = id (x) id   (contraction over the physical
%   index; the identity on the virtual pair is the paper's two clipped
%   "bracket" hooks: west pair tied by an east-facing bend, east pair tied by
%   a west-facing bend).
%   ink:  LHS = sandwich (A_i^{-1} over A_i, physical leg pairs automatically,
%         all four virtual ends open); RHS = a `east=cup` ghost cup (the ]
%         bracket: open west stubs, bend east) beside a `west=cup` ghost cup
%         (the [ bracket).
% Formula (second): (A_1^{-1} A_1)(A_2^{-1} A_2) contracted pairwise
%   = bracket · loop · bracket = D · (bracket bracket),
%   where the CLOSED loop (both cups on one ghost pair) is the trace of the
%   identity on the shared bond = its dimension D.
% Signature balance: eq 1 both sides (vw 2, ve 2, pu 0, pd 0);
%   eq 2 LHS (vw 2, ve 2) = bracket(ve 0, vw 2)+loop(0)+bracket(ve 2) — the
%   juxtaposed pictures carry the same total open-index multiset.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

One-sided inverse (1804.04964 lines 206--220): $A_i^{-1} A_i = \mathbb{I}$
\[
  \begin{tenkz}[sandwich]
    \tn{A_i^{-1}} \\
    \tn{A_i}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={wire,wire}, east=cup]
    \tnghost{}\\
    \tnghost{}
  \end{tenkz}
  \,
  \begin{tenkz}[rows={wire,wire}, west=cup]
    \tnghost{}\\
    \tnghost{}
  \end{tenkz}
\]

Pairwise contraction (1804.04964 lines 224--248):
$(A_1^{-1}A_1)(A_2^{-1}A_2) = D \cdot \mathbb{I}$
\[
  \begin{tenkz}[sandwich]
    \tn{A_1^{-1}} & \tn{A_2^{-1}} \\
    \tn{A_1} & \tn{A_2}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={wire,wire}, east=cup]
    \tnghost{}\\
    \tnghost{}
  \end{tenkz}
  \,
  % the closed bubble: both cups on one ghost pair — tr(id_D) = D
  \begin{tenkz}[rows={wire,wire}, west=cup, east=cup]
    \tnghost{}\\
    \tnghost{}
  \end{tenkz}
  \,
  \begin{tenkz}[rows={wire,wire}, west=cup]
    \tnghost{}\\
    \tnghost{}
  \end{tenkz}
  \;=\; D \cdot
  \begin{tenkz}[rows={wire,wire}, east=cup]
    \tnghost{}\\
    \tnghost{}
  \end{tenkz}
  \,
  \begin{tenkz}[rows={wire,wire}, west=cup]
    \tnghost{}\\
    \tnghost{}
  \end{tenkz}
\]
\end{document}
