% Regression: hard08_v2 — renders A^{i_1} A^{i_2} = sum_j U_{(i_1 i_2), j} A^j.
% T08 v2 — RMP arXiv:2011.12127, Eq. (RFPPure) / (AA=UA): the MPS
% renormalization-fixed-point condition.
%
% Formula:  A^{i_1} A^{i_2} = sum_j U_{(i_1 i_2), j} A^j
% for an isometry U: two adjacent MPS tensors equal a single tensor A
% followed by the isometry U on the physical index (1 input leg j from
% A below, 2 output legs i_1, i_2 up — one per blocked site).
%
% Declarative status: PARTIALLY BLOCKED.  The wide pill U draws ONE
% centred up leg, but the source needs one open leg per spanned column
% (i_1 at column 1, i_2 at column 2), so the two sides' boundary
% signatures disagree (LHS physical-up = 2, RHS physical-up = 1).
% Exact missing key: per-spanned-column legs for wide cells
% (`legs at={1,2}` on \tn[pill,wide=2]).  Also cosmetic: A below U is
% stretched to wide=2 so the U–A bond is vertical (the paper draws a
% unit-width A centred under U; a wide glyph cannot centre a normal
% neighbour).
%
% rows={op:none, ket}: the op row carries U (up leg open, down leg
% paired onto A); `:none` seals the op row's virtual boundary (U has no
% horizontal wires in the source); the ket row's A keeps the west/east
% virtual stubs, and its up leg is consumed by the pairing (= j).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[physical=up, tensor style=box]
    \tn{A} & \tn{A}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={op:none, ket}, tensor style=box]
    \tn[pill, wide=2]{U} & \\
    \tn[wide=2]{A} &
  \end{tenkz}
\]
\end{document}
