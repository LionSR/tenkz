% Regression: hard04_eig — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
% T04 — RMP fig9-1: eig( rho^R [double-layer chain] rho^L ) inside stretchy
% math parentheses; tests the slice contract (math-axis alignment) plus the
% boundary-vector-on-bent-bond genre again.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) slice-contract test with a plain grid picture inside \verb|\left(...\right)|:
\[
  \mathrm{eig}\left(
  \begin{tenkz}[rows={ket,bra}]
    \tn{} & \tn{} & \tndots & \tn{}\\
    \tn*{} & \tn*{} & \tndots & \tn*{}
  \end{tenkz}
  \right)
\]

(b) free-tier version of the actual figure (raw coordinates):
\[
  \mathrm{eig}\left(
  \begin{tenkzfree}
    \tnput[dot]{rR}{(-0.7,0)}{\rho^R}
    \tnput[dot]{k1}{(0,0.38)}{}  \tnput[dot]{k2}{(0.8,0.38)}{}
    \tnput[dot]{k3}{(1.6,0.38)}{} \tnput[dot]{k4}{(2.4,0.38)}{}
    \tnput[dot]{b1}{(0,-0.38)}{} \tnput[dot]{b2}{(0.8,-0.38)}{}
    \tnput[dot]{b3}{(1.6,-0.38)}{} \tnput[dot]{b4}{(2.4,-0.38)}{}
    \tnjoin{k1.east}{k2.west}\tnjoin{k2.east}{k3.west}\tnjoin{k3.east}{k4.west}
    \tnjoin{b1.east}{b2.west}\tnjoin{b2.east}{b3.west}\tnjoin{b3.east}{b4.west}
    \tnjoin{k1.south}{b1.north}\tnjoin{k2.south}{b2.north}
    \tnjoin{k3.south}{b3.north}\tnjoin{k4.south}{b4.north}
    \tnjoin[route=curve, out=180, in=90]{k1.west}{rR.north}
    \tnjoin[route=curve, out=270, in=180]{rR.south}{b1.west}
    \tnput[dot]{rL}{(3.1,0)}{\rho^L}
    \tnjoin[route=curve, out=0, in=90]{k4.east}{rL.north}
    \tnjoin[route=curve, out=90, in=0]{b4.east}{rL.south}
  \end{tenkzfree}
  \right)
\]
\end{document}
