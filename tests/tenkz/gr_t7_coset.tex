% Regression: gr_t7_coset — renders the coset action as k_yh and k_y wires followed by the V_{k_x^{-1}} V_{x_0}…
% Formula: [action of g on x] = [k_yh black bar; S-curved red k_y wire; gray
% Target 7 — arXiv:2203.12563 eq (21): the coset-decomposition composite
%   [action of g on x] = [k_yh black bar; S-curved red k_y wire; gray
%   staircase V_{k_x^{-1}} V_{x_0} ... ; middle A dot; mirrored right half]
% Four wire heights, six bars (two black, four gray) at staggered
% positions, two S-curved red wires, on-wire labels, below-bar labels.
% Free-layer attempt at the LEFT HALF only, boxes standing in for bars.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={op:operator, ket}]
    \tnghost{} & \tn{} & \tnghost{} \\
    \tnghost{} & \tn{} & \tnghost{}
  \end{tenkz}
  \;=\;
  \begin{tenkzfree}
    % black fusion bar splitting g -> k_y h (top), k_x^{-1} (mid)
    \tnput[box]{W}{(0mm,2mm)}{}
    % gray action bars, staircase
    \tnput[box]{Va}{(13mm,-6mm)}{}
    \tnput[box]{Vb}{(23mm,-1mm)}{}
    \tnjoin[label=g]{-6mm,2mm}{W.west}
    % k_y h leaves over the top; k_y S-curves down to the next picture
    \tnjoin[label={k_yh}]{W.north east}{20mm,6mm}
    \tnjoin[route=curve, out=0, in=180, label={k_y}]{20mm,6mm}{30mm,3mm}
    \tnjoin[label={k_x^{\text{-}1}}]{W.south east}{Va.north west}
    \tnjoin[label={x}]{0mm,-10mm}{Va.south west}
    \tnjoin[label={x_0}]{Va.east}{Vb.south west}
    \tnjoin[label={h}]{6mm,1mm}{Vb.north west}
    \tnjoin[label={x_0}]{Vb.east}{30mm,-1mm}
  \end{tenkzfree}
  \cdots
\]

\end{document}
