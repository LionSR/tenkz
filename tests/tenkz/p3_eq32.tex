% Regression: p3_eq32 — imported current-behavior fixture from the handoff corpus.
% Formula: (A_x^{-1} over A_y, physical index contracted) = delta_{x,y} )(
% p3 — arXiv:2203.12563 eq (32): the left inverse of the MPS tensor
%   (A_x^{-1} over A_y, physical index contracted) = delta_{x,y} )(
% LHS: sandwich pairing draws the contracted physical index between the
%   two beads; ghost cells extend the virtual wires; the bead labels take
%   their free quadrants (A_x^{-1} north, A_y south) automatically.
% RHS: the two cups are wire-only grid pictures closed by `east=cup'
%   (the ")" piece, stubs west) and `west=cup' (the "(" piece, stubs
%   east) — the 0.5 inter-layer cup primitive, no free-tier arcs needed.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[sandwich]
    \tnghost{} & \tn{A_x^{-1}} & \tnghost{} \\
    \tnghost{} & \tn{A_y} & \tnghost{}
  \end{tenkz}
  \;=\;\delta_{x,y}\,
  \begin{tenkz}[rows={wire, wire}, east=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
  \;
  \begin{tenkz}[rows={wire, wire}, west=cup]
    \tnghost{} \\
    \tnghost{}
  \end{tenkz}
\]

\end{document}
