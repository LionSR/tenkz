% Regression: gr_t8_sum — renders [staircase i,j] = sum_{c,mu,k} (L^y_abx)^{z,ij}_{c,k mu} [staircase k + bar mu].
% Formula: [staircase i,j] = sum_{c,mu,k} (L^y_abx)^{z,ij}_{c,k mu} [staircase k + bar mu]
% Target 8 — arXiv:2203.12563 eq (11): a diagram inside a sum with an
% L-symbol prefactor:
%   [staircase i,j] = sum_{c,mu,k} (L^y_abx)^{z,ij}_{c,k mu} [staircase k + bar mu]
% Math-atom discipline is the point: pictures as summands with coefficient.
% LHS staircase: two \tnfuse bars on overlapping row pairs (one fusion
% level each).  RHS needs a NESTED fusion (bar k feeds bar mu at a height
% between rows) — attempted with the same overlap trick.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={wire:operator, wire:operator, wire},
                bond label={$a$ at 1-3}]
    \tnfuse[combined=west]{} & & \tnghost{} \\
    & \tnfuse[combined=west]{} & \tnghost{} \\
    & & \tnghost{}
  \end{tenkz}
  \;=\;
  \sum_{c,\mu,k}
  \left(L^{y}_{abx}\right)^{z,ij}_{c,k\mu}\;
  \begin{tenkz}[rows={wire:operator, wire:operator, wire}]
    & \tnfuse[combined=west]{\mu} & \tnghost{} \\
    \tnfuse[combined=west]{} & & \tnghost{} \\
    & & \tnghost{}
  \end{tenkz}
\]

\end{document}
