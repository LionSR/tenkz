% Regression: plane_sweep2_v2 — tests derived separation and one explicit stress case.
% Formula: sep = (rows-1) x planesrise + planegap.
% plane_sweep2_v2: parts 1 and 3 of plane_sweep2 with the sheet sep
% overrides DELETED — every double layer takes the derived default
% sep = (rows-1) x planesrise + planegap.  Part W re-adds an explicit
% sheet sep=1.65 on a 5x5, which sits below the 5-row interleave
% threshold (4 x 0.45 = 1.8) and must emit both planes-interleave and
% sheet-coincide in the log.
\documentclass[varwidth=22cm, border=6pt]{standalone}
\usepackage{amsmath}
\usepackage{tenkz}
\begin{document}

\textbf{Part 1: 3x3, 2 open cells, derived default sep}

\medskip
D1 \texttt{default}\quad
\begin{tenkzplanes}[rows=3, cols=3, open={(1,2),(3,3)}]
\end{tenkzplanes}
\qquad
D2 \texttt{default}\quad
\begin{tenkzplanes}[rows=3, cols=3, open={(1,2),(3,3)}]
\end{tenkzplanes}

\bigskip
\textbf{Part 3: 4x4, open on a full column, derived default sep}

\medskip
C1 \texttt{default}\quad
\begin{tenkzplanes}[rows=4, cols=4, open={(1-4,2)}]
\end{tenkzplanes}
\qquad
C2 \texttt{default}\quad
\begin{tenkzplanes}[rows=4, cols=4, open={(1-4,2)}]
\end{tenkzplanes}

\bigskip
\textbf{Part W: 5x5, explicit sheet sep=1.65 (below threshold, must warn)}

\medskip
W1 \texttt{sep=1.65}\quad
\begin{tenkzplanes}[rows=5, cols=5, sheet sep=1.65]
\end{tenkzplanes}

\end{document}
