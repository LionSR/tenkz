% Regression: t2_hamilcommu — renders (rho = the 6-site MPO word A x A x A x A x A x A on the red.
% Formula: (rho = the 6-site MPO word A x A x A x A x A x A on the red
% T2 — RMP arXiv:2011.12127, fig. fig3-pepe_hamilcommu: the local
% Hamiltonian term commutes with the boundary MPO word.
% Formula (rho = the 6-site MPO word A x A x A x A x A x A on the red
% virtual wire, h = the two-site gate on sites 3,4):
%     (h (x) 1_rest) . rho  =  rho . (h (x) 1_rest)
% LHS ink: h attached from ABOVE to the up legs of sites 3,4 (its own two
% outer legs stay open above); RHS ink: the same h attached from BELOW to
% the down legs of sites 3,4 (outer legs open below).  All other sites
% keep both physical legs open; both virtual ends stay open (red stubs).
%
% Declarative reading attempted here: a gate row over/under an operator
% row -- rows={op:none, op:operator} with h = \tn[pill, wide=2,
% legs at={1,2}] at columns 3-4 and \tnskip holes elsewhere (a hole keeps
% the partner site's facing leg open, which is exactly what sites
% 1,2,5,6 need).
%
% VERIFIED BLOCKER (this file compiles; the render is wrong at the gate):
% `legs at=` is honored only for OPEN legs.  At a PAIRED interface the
% pairing pass draws one wire from the wide glyph's centre-south to the
% single base column below (a diagonal), and the second spanned column's
% partner keeps no leg at all -- a wide gate cannot drop one paired leg
% per spanned column.  Missing primitive: per-spanned-column pairing for
% wide glyphs (`legs at=` at a paired interface).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: h . rho -- gate above, contracted with the up legs of sites 3,4
  \begin{tenkz}[rows={op:none, op:operator}, tensor style=box]
    \tnskip & \tnskip & \tn[pill, wide=2, legs at={1,2}]{h} & & \tnskip
      & \tnskip \\
    \tn{} & \tn{} & \tn{} & \tn{} & \tn{} & \tn{}
  \end{tenkz}
  \;=\;
  % RHS: rho . h -- the same gate below, contracted with the down legs
  \begin{tenkz}[rows={op:operator, op:none}, tensor style=box]
    \tn{} & \tn{} & \tn{} & \tn{} & \tn{} & \tn{} \\
    \tnskip & \tnskip & \tn[pill, wide=2, legs at={1,2}]{h} & & \tnskip
      & \tnskip
  \end{tenkz}
\]
\end{document}
