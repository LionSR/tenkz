% Regression: hard10_pta — renders P_{T(A)} = sum_{i=1}^n [left-canonical triangles ... GAP at site i.
% Formula: P_{T(A)} = sum_{i=1}^n [left-canonical triangles ... GAP at site i ...
% T10 — RMP PTA: the tangent-space projector.
% P_{T(A)} = sum_{i=1}^n [left-canonical triangles ... GAP at site i ...
% right-canonical triangles, layers cup-closed at both ends]
%          - sum_{i=1}^n [same with the gap filled and a mid cut].
% Multi-term equation of two-layer diagrams with holes.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) grid attempt at ONE summand (4 sites, gap at site 3): the gap in the bra
layer cannot be expressed — \verb|\tnghost| still routes the wire INTO the
ghost (the hole's virtual indices must stay open), it silently deletes the
ket tensor's physical leg at that column, and an absent cell just shortens
the row; boundary cups and triangles are missing as before:
\[
  P_{T(A)} \;=\; \sum_{i=1}^{n}\;
  \begin{tenkz}[rows={ket,bra}]
    \tn{A_L} & \tn{A_L} & \tn{A_R} & \tn{A_R}\\
    \tn*{A_L} & \tn*{A_L} & \tnghost{} & \tn*{A_R}
  \end{tenkz}
  \;-\; \sum_{i=1}^{n}\;
  \begin{tenkz}[rows={ket,bra}]
    \tn{A_L} & \tn{A_L}\tncut{} & \tn{A_R} & \tn{A_R}\\
    \tn*{A_L} & \tn*{A_L} & \tn*{A_R} & \tn*{A_R}
  \end{tenkz}
\]
\end{document}
