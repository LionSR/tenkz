% Target: rmp-app-czx-state
% Formula: |CZX> is obtained by blocking the plaquette GHZ product into sites.
% Ink: Four neutral outlined blocks overlap a three-by-three array of red sites.
% Boundary: The displayed patch has open virtual bonds on every outer lattice site.
% Source: paper TN-Review-main.tex line 2511; author source ImagesReview Section
%   V/ImagesReview Section V.tex lines 147-219
% Capabilities: lattice, regions, semantic-roles
\[
  \begin{tenkzlattice}[rows=3, cols=3, frame=flat, boundary=open]
    \tnsite[role=operator]{(1,1)} \tnsite[role=operator]{(1,2)}
    \tnsite[role=operator]{(1,3)} \tnsite[role=operator]{(2,1)}
    \tnsite[role=operator]{(2,2)} \tnsite[role=operator]{(2,3)}
    \tnsite[role=operator]{(3,1)} \tnsite[role=operator]{(3,2)}
    \tnsite[role=operator]{(3,3)}
    \tnregion[slot=neutral, outline]{(1-2,1-2)}
    \tnregion[slot=neutral, outline]{(1-2,2-3)}
    \tnregion[slot=neutral, outline]{(2-3,1-2)}
    \tnregion[slot=neutral, outline]{(2-3,2-3)}
  \end{tenkzlattice}
\]
