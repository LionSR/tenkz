% Target: rmp-iii-b-condensation
% Formula: A doubled anyon string joins paired endpoints on ket and bra sheets.
% Ink: Canonical public tenkzplanes with the same marked path on both sheets.
% Boundary: Both 6x4 sheets are open; paired string endpoints stay uncontracted.
% Source: paper TN-Review-main.tex line 1697; author source ImagesReview Section
%   III/ImagesReview Section III.tex lines 925-953
% Capabilities: lattice, double-layer-string, marked-endpoints
\[
  % The author source repeats one path at y=-0.2 and y=0.2. The named oblique
  % frame separates the same-x ket/bra markers; the endpoint interfaces remain
  % open.
  \begin{tenkzplanes}[rows=6, cols=4,
                      frame=oblique,
                      sheet vector={0mm,2.70mm},
                      open={(1,2),(5,3)}]
    \tnsite[role=marked, size=l]{(1,2,1)}
    \tnsite[role=marked,size=s]{(2,2,1)} \tnsite[role=marked,size=s]{(3,2,1)}
    \tnsite[role=marked,size=s]{(4,2,1)} \tnsite[role=marked,size=l]{(5,3,1)}
    \tnsite[role=marked, size=l]{(1,2,0)}
    \tnsite[role=marked,size=s]{(2,2,0)} \tnsite[role=marked,size=s]{(3,2,0)}
    \tnsite[role=marked, size=s]{(4,2,0)}
    \tnsite[role=marked, size=l]{(5,3,0)}
    \tnedge[role=operator]{(1,2,1)-(2,2,1)}
    \tnedge[role=operator]{(2,2,1)-(3,2,1)}
    \tnedge[role=operator]{(3,2,1)-(4,2,1)}
    \tnedge[role=operator]{(4,2,1)-(5,3,1)}
    \tnedge[role=operator]{(1,2,0)-(2,2,0)}
    \tnedge[role=operator]{(2,2,0)-(3,2,0)}
    \tnedge[role=operator]{(3,2,0)-(4,2,0)}
    \tnedge[role=operator]{(4,2,0)-(5,3,0)}
  \end{tenkzplanes}_{\ a\leftrightarrow\bar a}
\]
