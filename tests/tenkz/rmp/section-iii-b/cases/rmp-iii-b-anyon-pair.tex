% Target: rmp-iii-b-anyon-pair
% Formula: |psi(a,a^*)> is an open anyon string with endpoints i and i^*.
% Ink: Canonical public tenkzlattice with a distinguished endpoint path.
% Boundary: The 3x3 PEPS patch retains all twelve perimeter virtual stubs and
%   nine physical legs; both anyon string endpoints remain marked.
% Source: paper TN-Review-main.tex line 1645; author source ImagesReview Section
%   III/ImagesReview Section III.tex lines 644-660
% Capabilities: lattice, path-string, marked-endpoints
\[
  % The lattice owns the projected sheet and keeps every physical carrier
  % vertical in the paper frame.  The public integer-cell surface cannot yet
  % place the source's two half-grid beads, so the endpoint path stays coarse.
  \begin{tenkzlattice}[rows=3, cols=3, frame=oblique,
                       physical=up, boundary=open]
    \tnsite[role=marked, size=l, label={$i$},
            label pos=north west]{(1,3)}
    \tnsite[role=marked, size=l, label={$i^{*}$},
            label pos=south east]{(3,1)}
    \tnedge[role=operator, label={$a$}]{(3,1)-(1,3)}
  \end{tenkzlattice}
\]
