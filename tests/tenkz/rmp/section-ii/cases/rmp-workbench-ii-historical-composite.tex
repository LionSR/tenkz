% Target: rmp-workbench-ii-historical-composite
% Formula: The archival composite pairs one-to-two MPS and one-to-four PEPS splittings.
% Ink: Kernel flat and plane frames; the archival arrows and panel letters stay
%   plain math outside the audited kernel scope.
% Boundary: Both archival arrows preserve their open virtual and physical boundary
%   signatures.
% Source: author source ImagesReview Section II/ImagesReview Section II.tex lines
%   801-831; archival output ImagesReview Section II.pdf
% Capabilities: kernel, plane-frame
\tenkzkernel
\[
  \begin{gathered}
    (a)\quad
    % one MPS site: 3-leg dot
    \begin{tenkz}[rows={wire}, cols=1, physical=up, west=open, east=open]
      \tn{}
    \end{tenkz}
    \;\longrightarrow\;
    % the split: two sites of the same bond dimension
    \begin{tenkz}[rows={wire}, cols=2, physical=up, west=open, east=open]
      \tn{} & \tn{}
    \end{tenkz}
    \\[1em]
    (b)\quad
    % one PEPS site: 5-leg tensor on the projected sheet
    \begin{tenkz}[lattice={1x1}, frame=plane, physical=up,
                  west=open, east=open, north=open, south=open]
      \tn{}
    \end{tenkz}
    \;\longrightarrow\;
    % the splitting: a 2x2 patch with the same outer boundary signature
    \begin{tenkz}[lattice={2x2}, frame=plane, physical=up,
                  west=open, east=open, north=open, south=open]
      \tn{} & \tn{} \\
      \tn{} & \tn{}
    \end{tenkz}
  \end{gathered}
\]
