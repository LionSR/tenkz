% Target: rmp-workbench-ii-peps-fine-graining
% Formula: One PEPS tensor is fine-grained into a 2x2 tensor patch.
% Ink: Kernel plane frames on both sheets; the fine-graining arrow between the
%   panels stays plain math outside the audited kernel scope.
% Boundary: The one-site and 2x2 sheets expose matching outer virtual and physical
%   boundaries.
% Source: author source ImagesReview Section II/ImagesReview Section II.tex lines
%   801-831; archival output figrenorm2D.pdf
% Capabilities: kernel, plane-frame
\tenkzkernel
\[
  % one PEPS site: 5-leg tensor on the projected sheet
  \begin{tenkz}[lattice={1x1}, frame=plane, physical=up,
                west=open, east=open, north=open, south=open]
    \tn{}
  \end{tenkz}
  \;\longrightarrow\;
  % the fine-graining: a 2x2 patch with the same outer boundary signature
  \begin{tenkz}[lattice={2x2}, frame=plane, physical=up,
                west=open, east=open, north=open, south=open]
    \tn{} & \tn{} \\
    \tn{} & \tn{}
  \end{tenkz}
\]
