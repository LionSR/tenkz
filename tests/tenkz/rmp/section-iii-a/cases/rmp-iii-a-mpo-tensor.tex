% Target: rmp-iii-a-mpo-tensor
% Formula: the 4-valent MPO tensor — red virtual MPO wire a: j -> k
% Ink: Kernel flat frames; the grid panel types the legs and the fat-graph
%   panel directs them.
% Boundary: Resolved by the public environment; open indices remain explicit.
% Source: paper TN-Review-main.tex line 1341; paper-context-only; no author drawing
%   source
% Capabilities: kernel, typed-ports
\tenkzkernel
% panel 1 — the same tensor in the grid language: an MPO site with typed open
% legs j (west), k (east) and physical legs m (up), n (down).
\[
  \begin{tenkz}[rows={wire}, cols=1, bonds=none]
    \tn[name=T, skin=dot, ports={180:virtual:$j$, 0:virtual:$k$,
      90:physical:$m$, 270:physical:$n$}]{}
  \end{tenkz}
\]

% panel 2 — the fat-graph depiction: two directed matrix levels meeting at a
% glyphless junction (the fat graph IS the tensor).  The grid panel above owns
% the leg typing, so each fat wire is one directed level; the junction sits
% east of the level midpoint, so the level name a reads over clear paper.
\[
  \begin{tenkz}[rows={wire,wire,wire}, cols=4, bonds=none]
    \tn[at=(2,1), name=j, skin=dot, label pos=180, ports={0:virtual}]{j}
    \tn[at=(2,4), name=k, skin=dot, label pos=0, ports={180:virtual}]{k}
    \tn[at=(1,3), name=m, skin=dot, label pos=90, ports={270:virtual}]{m}
    \tn[at=(3,3), name=n, skin=dot, label pos=270, ports={90:virtual}]{n}
    \tn[at=(2,3), name=X, skin=none,
      ports={180:virtual, 0:virtual, 90:virtual, 270:virtual}]{}
    \tnwire[name=a]{j.0}{X.180}
    \tnwire[dir=to]{X.0}{k.180}
    \tnwire{n.90}{X.270}
    \tnwire[dir=to]{X.90}{m.270}
    \tnmark[form=label, label pos=90]{on a 0.5}{$a$}
  \end{tenkz}
\]
