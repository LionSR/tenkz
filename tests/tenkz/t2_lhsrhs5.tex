% Regression: t2_lhsrhs5 — imported current-behavior fixture from the handoff corpus.
% Formula: (the step after inverting the injective pair (A,B), inverse S):
% t2_lhsrhs5 — RMP arXiv:2011.12127, parent-Hamiltonian intersection
% argument, fifth displayed step, files fig4_fig4-pepe_LHS5.pdf /
% RHS5.pdf.
%
% Formula (the step after inverting the injective pair (A,B), inverse S):
% the boundary-condition constraint collapses to
%   sum  B^{i_2} (open bond) (open i_3) (open bond) C^{i_4} . R-loop
%   =    (open bond) C . X-loop,
% i.e. site 3 of the LHS word is REMOVED (its tensor inverted away): the
% two virtual bonds at the removed site and its physical index all stay
% open; on the RHS everything west of C is removed and absorbed into the
% new boundary tensor X.
%
% Declarative reading (rows={ket:nopair, bra}):
%   * \tnskip is the removed site: a hole whose column keeps BOTH
%     virtual wires open and its physical index open (exactly the
%     tangent-space-summand semantics, hard10 genre);
%   * wide pill in the bra row = the boundary tensor (R resp. X), cups
%     tie the surviving word ends into it; the RHS has only an east cup
%     (west of C nothing survives: an EMPTY leading cell, C's west bond
%     stays open as a stub).
%
% Noted gaps:
%   * the reference draws a hole's open virtual indices as wires TURNED
%     UP into vertical stubs; \tnskip leaves them as horizontal stubs at
%     wire height — same open indices, different routing;
%   * the boundary tensor's escaping physical leg is emitted down from
%     the bra row (see t2_lhsrhs1);
%   * RHS boundary-seal gap: the reference seals X's WEST end while its
%     east end joins C; the grid cannot spell this — the rows= suffix
%     `none` seals BOTH sides of a row and makes it cup-INELIGIBLE
%     (pair-selection rule), and west=/east= act on ALL rows.  Missing
%     primitive: per-row per-side boundary policy (a `west none`-style
%     rows= suffix).  As rendered, X keeps a spurious open west stub;
%   * source-figure imbalance: the reference's two sides do not show the
%     same open-index multiset (the LHS hole's indices have no RHS
%     counterpart) — the tenkz boundary signatures make this visible in
%     the .tnlog rather than hiding it.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: (wire) B [hole] C over the boundary tensor R
  \begin{tenkz}[rows={ket:nopair, bra}, west=cup, east=cup,
                tensor style=box]
    \tnghost{} & \tn{B} & \tnskip & \tn{C}\\
    \tn[pill, wide=4]{R} & & &
  \end{tenkz}
  \;=\;
  % RHS: C alone (west bond open) over the boundary tensor X
  \begin{tenkz}[rows={ket:nopair, bra}, east=cup, tensor style=box]
    & \tn{C}\\
    \tn[pill, wide=2]{X} &
  \end{tenkz}
\]
\end{document}
