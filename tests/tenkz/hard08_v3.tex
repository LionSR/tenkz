% Regression: hard08_v3 — imported current-behavior fixture from the handoff corpus.
% T08 v3 — RMP arXiv:2011.12127, Eq. (AA=UA): the MPS
% renormalization-fixed-point condition.  Previously BLOCKED on
% per-spanned-column legs; now expressed with `legs at={1,2}`.
%
% Formula:  A^{i_1} A^{i_2} = sum_j U_{(i_1 i_2), j} A^j
% for an isometry U on the blocked physical index: two adjacent MPS
% tensors equal one tensor A (physical index j, consumed by the
% pairing) followed by U, which keeps ONE output leg per blocked site
% (i_1 over spanned column 1, i_2 over spanned column 2).
%
% Declarative reading:
%   rows={op:none, ket} — U rides the op row (up legs open, down leg
%   paired onto A below = the summed index j); `:none` seals the op
%   row's virtual boundary (U has no horizontal wires in the source);
%   the ket row keeps A's west/east virtual stubs.
%   legs at={1,2} — one open up leg per named spanned column of the
%   wide pill, so both sides read physical-up = 2 (v2's blocker).
%   A at wide=2 — the box skin keeps unit width; wide only centres the
%   glyph under U, so the U–A bond is vertical, as in the paper.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: A^{i_1} A^{i_2} — two MPS tensors, one open up leg each
  \begin{tenkz}[physical=up, tensor style=box]
    \tn{A} & \tn{A}
  \end{tenkz}
  \;=\;
  % RHS: sum_j U_{(i_1 i_2), j} A^j — U keeps one leg per blocked
  % site; the vertical U–A bond is the summed physical index j
  \begin{tenkz}[rows={op:none, ket}, tensor style=box]
    \tn[pill, wide=2, legs at={1,2}]{U} & \\
    \tn[wide=2]{A} &
  \end{tenkz}
\]
\end{document}
