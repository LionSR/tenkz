% Regression: cups_edge — exercises inter-layer cups with odd, unpaired, operator, and fused rows.
% Formula: geometry-only; no tensor identity.
% cups_edge — corner cases of the inter-layer cups.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

E1 odd row out (three open rows, east=cup): rows 1-2 cup, row 3 keeps
its stub:
\[
  \begin{tenkz}[rows={ket:nopair, bra, wire}, east=cup]
    \tn{A} & \tn{A}\\
    \tn*{A} & \tn*{A}\\
    \tnX{X} & \tnghost{}
  \end{tenkz}
\]

E2 the side policy outranks the picture policy: boundary=none seals the
sides that declare nothing, and east=cup ties the east ends:
\[
  \begin{tenkz}[sandwich, boundary=none, east=cup]
    \tn{A}\\
    \tn*{A}
  \end{tenkz}
\]

E3 one word per side, last wins: east=cup retracts the east half of the
periodic alias, leaving a west trace with no partner (warned, not drawn)
and an east cup:
\[
  \begin{tenkz}[sandwich, periodic, east=cup]
    \tn{A}\\
    \tn*{A}
  \end{tenkz}
\]

E4 per-row opt-out: the bra row is sealed, so only ket+op rows remain
eligible and cup 1-2:
\[
  \begin{tenkz}[rows={op:nopair, op, bra:none}, west=cup]
    \tn[mpo]{O} & \tn[mpo]{O}\\
    \tn[mpo]{P} & \tn[mpo]{P}\\
    \tn{A} & \tn{A}
  \end{tenkz}
\]

E5 rows of unequal length: the shorter row gets a horizontal lead-in, the
turn stays one smooth half-ellipse:
\[
  \begin{tenkz}[rows={ket:nopair, bra}, east={cup=$\rho$}]
    \tn{A} & \tn{A} & \tn{A}\\
    \tn*{A} & \tn*{A}
  \end{tenkz}
\]

E6 layer sep stretches the cup vertically (still smooth):
\[
  \begin{tenkz}[sandwich, layer sep=1.8, west=cup, east=cup]
    \tn{A} & \tn{A}\\
    \tn*{A} & \tn*{A}
  \end{tenkz}
\]

E7 fuse consumes its side: the west end of rows 1-2 is the fusion bar, so
west=cup finds no eligible pair there and only the ket/bra stubs east
cup:
\[
  \begin{tenkz}[rows={op, ket}, east=cup]
    \tnfuse[span=2]{V} & \tn[mpo]{O} & \tn{A}\\
                       & \tn{A} &
  \end{tenkz}
\]
\end{document}
