% Regression: zz_edgeprobe — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=20cm, border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=3, cols=4, boundary legs]
  \tnedge[distinguished]{(1,1)-(1,2)}
  \tnedge[style=dashed]{(2,1)-(2,2)}
  \tnedge[label=$g$]{(3,2)-(3,3)}
  \tnedge[role=operator]{(1,3)-(2,3)}
  \tnedge[role=marked, style={dashed}, label=$h$]{(2,4)-(3,4)}
  \tnedge[none]{(1,1)-(2,1)}
\end{tenkzlattice}
\end{document}
