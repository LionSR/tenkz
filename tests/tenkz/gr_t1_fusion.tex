% Regression: gr_t1_fusion — imported current-behavior fixture from the handoff corpus.
% Formula: T_a T_b = sum_{c,mu} What T_c W   (fat bars, red MPO wires, mu labels)
% Target 1 — arXiv:2203.12563 eq (3): fusion-tensor decomposition
%   T_a T_b = sum_{c,mu} What T_c W   (fat bars, red MPO wires, mu labels)
% LHS: grid, two operator-hued op rows (paired physical wire through both).
% RHS attempt A: grid with \tnfuse — the fused wire c is a stub between the
%   rows, so the middle T_c dot cannot sit on it.  Best-possible grid form.
% RHS attempt B: free layer — no fat-bar glyph, no per-wire hue.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

% --- the equation, best-possible attempt -------------------------------
\[
  \begin{tenkz}[rows={op:operator, op:operator},
                bond label={$a$ at 1-2}]
    \tnghost{} & \tn{} & \tnghost{} \\
    \tnghost{} & \tn{} & \tnghost{}
  \end{tenkz}
  \;=\; \sum_{c,\mu}\;
  \begin{tenkz}[rows={wire:operator, wire:operator}]
    \tnghost{} & \tnfuse[combined=east]{\mu} &
      \tnfuse[combined=west]{\mu} & \tnghost{} \\
    \tnghost{} & & & \tnghost{}
  \end{tenkz}
\]

% --- RHS attempt B: free layer, faking the bars with box glyphs --------
\[
  \sum_{c,\mu}\;
  \begin{tenkzfree}
    \tnput[box, ports={west:virtual, east:virtual}]{W}{(0,0)}{}
    \tnput[dot]{T}{(8mm,0)}{}
    \tnput[box, ports={west:virtual, east:virtual}]{V}{(16mm,0)}{}
    \tnjoin[label=c]{W.east}{T.west}
    \tnjoin[label=c]{T.east}{V.west}
    \tnjoin{-5mm,3mm}{W.west}
    \tnjoin{-5mm,-3mm}{W.west}
    \tnjoin{V.east}{21mm,3mm}
    \tnjoin{V.east}{21mm,-3mm}
    \tnjoin{T.north}{8mm,4mm}
    \tnjoin{T.south}{8mm,-4mm}
  \end{tenkzfree}
\]

\end{document}
