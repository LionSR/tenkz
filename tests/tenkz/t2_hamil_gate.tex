% Regression: t2_hamil_gate — renders (rho = the 6-site MPO word A^(x6) on the red virtual wire.
% Formula: (rho = the 6-site MPO word A^(x6) on the red virtual wire,
% T2 — RMP arXiv:2011.12127, fig. fig3-pepe_hamilcommu: the local
% Hamiltonian term commutes with the MPO word.
% Formula (rho = the 6-site MPO word A^(x6) on the red virtual wire,
% h = the two-site gate on sites 3,4):
%     (h (x) 1_rest) . rho  =  rho . (h (x) 1_rest)
% LHS ink: grey pill h contracted from ABOVE with the up legs of sites 3,4
% (its own outer legs open above); RHS ink: the same pill contracted from
% BELOW with the down legs of sites 3,4.  All other sites keep both
% physical legs open; both virtual ends stay open (red stubs).
%
% Declarative attempt: gate row over/under the operator row --
% rows={op:none, op:operator}, h = \tn[pill, wide=2, legs at={1,2}] at
% columns 3-4, \tnskip holes elsewhere (a hole keeps the facing partner
% leg open, which sites 1,2,5,6 need).
%
% Checked against tenkz-grid.code.tex (leg pass, \tenkz_draw_legs:): at a
% PAIRED interface the wide glyph draws ONE wire from its centre-south to
% the base column's partner (a diagonal), and the second spanned column's
% partner keeps no leg -- `legs at=` multi-legs exist only for OPEN legs
% (\tenkz_leg_open:nnn branches on clegsat only in the open-stub pass).
% Also: the \tnskip holes make the pill sprout spurious west/east virtual
% stubs (hole semantics), absent from the source.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: h . rho -- gate above, contracted with the up legs of sites 3,4
  \begin{tenkz}[rows={op:none, op:operator}, tensor style=box]
    \tnskip & \tnskip & \tn[pill, wide=2, legs at={1,2}]{h} & & \tnskip
      & \tnskip \\
    \tn{} & \tn{} & \tn{} & \tn{} & \tn{} & \tn{}
  \end{tenkz}
  \;=\;
  % RHS: rho . h -- the same gate below, contracted with the down legs
  \begin{tenkz}[rows={op:operator, op:none}, tensor style=box]
    \tn{} & \tn{} & \tn{} & \tn{} & \tn{} & \tn{} \\
    \tnskip & \tnskip & \tn[pill, wide=2, legs at={1,2}]{h} & & \tnskip
      & \tnskip
  \end{tenkz}
\]
\end{document}
