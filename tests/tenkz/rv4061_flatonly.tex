% Regression: rv4061_flatonly — exercises grid pictures with `rows={wire}, cols=2` through \tn.
% Formula: geometry-only; no tensor identity.
\documentclass{article}\usepackage{tenkz}\begin{document}
X1\begin{tenkz}[rows={wire}, cols=2] \tn{A} & \tn{B} \end{tenkz}X2
\end{document}
