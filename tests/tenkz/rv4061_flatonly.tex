% Regression: rv4061_flatonly — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
\documentclass{article}\usepackage{tenkz}\begin{document}
X1\begin{tenkz}[rows={wire}] \tn{A} & \tn{B} \end{tenkz}X2
\end{document}
