% Regression: hard11_circuit — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
% T11 — RMP sec2fig11b: staircase circuit of two-site gates preparing a
% state from |0...0>, MPU/circuit genre (vertical time axis, gates
% spanning two columns, vertical wires passing between non-adjacent rows).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) grid attempt — gates as wide boxes on op rows; the staircase offset and
the pass-through vertical wires between non-adjacent layers cannot be said:
\[
  \begin{tenkz}[rows={op,op,op}, tensor style=box]
    \tn[wide=2]{U_1} & & \tn[wide=2]{U_2} & \\
    & \tn[wide=2]{U_3} & & \\
    & & \tn[wide=2]{U_4} &
  \end{tenkz}
\]

(b) free-tier attempt — raw coordinates:
\[
  \begin{tenkzfree}
    \tnput[box]{u1}{(0.5,1.5)}{U_1}
    \tnput[box]{u2}{(1.5,1.0)}{U_2}
    \tnput[box]{u3}{(2.5,0.5)}{U_3}
    \tnput[dot]{z}{(3.2,-0.2)}{|0\rangle}
    \tnjoin{u1.south}{0.5,-0.2}
    \tnjoin{1.2,1.3}{u2.north}
    \tnjoin{u2.south}{1.5,-0.2}
    \tnjoin{2.2,0.8}{u3.north}
    \tnjoin{u3.south}{2.5,-0.2}
    \tnjoin{0,1.9}{u1.north}
    \tnjoin{1,1.9}{1,1.7}
  \end{tenkzfree}
\]
(the two-column extent of each gate, the per-column input/output wires, and
the staircase wiring have to be faked; boxes here have single north/south
anchors, so multi-wire gates are not honestly drawable even in the free tier)
\end{document}
