% Regression: hard04_v3 — imported current-behavior fixture from the handoff corpus.
% hard04 v3 — spectrum of the n-site reduced density matrix (RMP fig9-1),
% re-attempted with the upgraded `west=cup` / `east=cup` keys.
%
% Formula:  eig( rho_n ),  with rho_n as in sec2fig8:
%   [rho_n]_{(s),(s')} = Tr[ rho^R A^{s_1}..A^{s_n} rho^L (A^{s'_1}..A^{s'_n})^dagger ]
%
% The picture is the sec2fig8 racetrack (ket rail legs up, bra rail legs
% down, rho^R / rho^L on the end bends) inside stretchy math parentheses —
% this also exercises the slice contract: the picture's wire axis must sit
% on the math axis so \left( ... \right) centres on the racetrack.
%
% Signature: (vw 0, ve 0, pu 4, pd 4); eig(...) is algebra around the one
% picture, so there is no picture-to-picture balance to check.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \mathrm{eig}\left(
  % the rho_n racetrack: open legs (nopair), both virtual ends closed
  % through the on-bend fixed points rho^R (west) and rho^L (east)
  \begin{tenkz}[rows={ket:nopair, bra},
                west={cup=$\rho^R$}, east={cup=$\rho^L$}]
    \tn{} & \tn{} & \tn{} & \tn{}\\
    \tn*{} & \tn*{} & \tn*{} & \tn*{}
  \end{tenkz}
  \right)
\]
\end{document}
