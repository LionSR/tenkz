% Regression: p3_min1 — exercises grid pictures with `rows={op:operator, op:operator}` through \tn.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[rows={op:operator, op:operator}]
    \tn{T_a} \\
    \tn{T_b}
  \end{tenkz}
\]
\end{document}
