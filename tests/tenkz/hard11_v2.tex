% Regression: hard11_v2 — imported current-behavior fixture from the handoff corpus.
% T11 v2 — RMP sec2fig11b: state prepared from |0> by a widening
% tree/brick circuit of two-site gates (sequential generation).
% Formula: |psi> = ( prod_{layers L..1} (x)_k U^{(l)}_k ) |0>,
% each U a two-site gate spanning two wires; successive layers overlap
% brick-fashion, and the bottom funnel doubles the wire count per layer
% starting from the single |0> wire.
%
% Declarative status: a circuit is a grid (rows = wires, cols = layers),
% but a two-site gate is one atom TERMINATING TWO ROWS' WIRES — the spec
% key `wires=<k>` on \tn (quantikz \gate[2]) — which is unimplemented.
% Without it a wide box gets a single centred leg, gates in one row are
% joined by spurious horizontal bonds, and the brick offsets have no
% wiring.  The nearest implemented object is the fusion-tree generator
% \tntree: it draws the funnel's binary-tree SKELETON (trivalent
% junction dots for the doubling maps, a rooted leg for |0>), losing the
% gates-as-boxes and the brick overlaps between layers.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % 8-leaf binary tree rooted in |0>: the doubling funnel of the figure
  \tntree{ ((({}\,{})\,({}\,{}))\,(({}\,{})\,({}\,{})))_{|0\rangle} }
\]
\end{document}
