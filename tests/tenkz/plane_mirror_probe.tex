% Regression: plane_mirror_probe — renders probe: the literal x = c*lp + r*slant*lp (negated slant coefficient).
% Formula: probe: the literal x = c*lp + r*slant*lp (negated slant coefficient)
% probe: the literal x = c*lp + r*slant*lp (negated slant coefficient)
\documentclass[varwidth=20cm, border=6pt]{standalone}
\usepackage{tenkz}
\makeatletter\def\tenkz@r@planeslant{-0.45}\makeatother
\begin{document}
\begin{tenkzlattice}[rows=4, cols=4, frame=oblique, physical=up, boundary legs]
\end{tenkzlattice}
\end{document}
