% Regression: gr_t5_zipper — renders W(gh) [n MPO columns on wires g,h] What(gh) = [n sites on one wire gh].
% Formula: W(gh) [n MPO columns on wires g,h] What(gh)  =  [n sites on one wire gh]
% Target 5 — arXiv:2203.12563 eq (28): the zipper / pulling-through
%   W(gh) [n MPO columns on wires g,h] What(gh)  =  [n sites on one wire gh]
% with an n-brace over the columns on BOTH sides.
% Grid attempt: two operator-hued op rows; \tnfuse closes both ends;
% \tnspan for the braces; bond label fakes the gh wire-end label.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={op:operator, op:operator}]
    \tnfuse{gh} & \tn{}\tnspan{2}{n} & \tn{} &
      \tnfuse[combined=east]{gh} \\
    & \tn{} & \tn{} &
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={op:operator}, bond label={$gh$ at 2-3}]
    \tn{}\tnspan{2}{n} & \tn{} & \tnghost{}
  \end{tenkz}
\]

\end{document}
