% Regression: t2_wrap9a — imported current-behavior fixture from the handoff corpus.
% Formula: (bottom):  sum_{{beta}} O^{(1)}_{..., beta} O^{(2)}_{..., beta} —
% t2_wrap9a — RMP arXiv:2011.12127, file fig2_fig2-pepe_sec2fig9a as it now
% stands: (top) one 4 x 4 PEPO window; (bottom) TWO stacked PEPO sheets
% whose east boundary bonds close pairwise sheet-to-sheet by wrap loops —
% the torus/wrap depiction of contracting the two operator layers' cut
% virtual indices at the matching edge.
%
% Formula (bottom):  sum_{{beta}} O^{(1)}_{..., beta} O^{(2)}_{..., beta} —
%   the east virtual multi-index beta of the upper sheet is contracted with
%   the east virtual multi-index of the lower sheet (one wrap per row);
%   every physical index and every other boundary bond stays open.
%
% Declarative target (bottom):  a stacked-sheet environment with an
% INTER-SHEET VIRTUAL closure at one side, e.g.
%   \begin{tenkzplanes}[rows=4, cols=4, east=cup]
% — a `east=cup` acting on the sheets' east BOUNDARY BONDS, the planes
% analog of the grid cup.
%
% STILL-BLOCKED, one key:
%  * inter-sheet virtual closure: /tenkz/planes/east=cup does not exist;
%    the only inter-sheet tie tenkzplanes draws is trace={(r,c),...}, which
%    closes a site's PHYSICAL pairing pair (Tr_s), not the sheets' east
%    boundary bonds ("inter-sheet closures" is on the WHATS-NEW-0.5 gap
%    list, tracked for 0.6).
% RESOLVED (0.6): /tenkz/lattice/physical/updown (the PEPO up+down site
% legs) landed — see t2_pepo5 / t2_probe_pepoupdown; a bare PEPO sheet
% can now be stated.  In a role-list stack the op sheet's up+down faces
% come from the pairing/outer machinery, so this file still waits only
% on the closure key.
%
% The picture below is the NEAREST EXISTING SPELLING of the bottom panel,
% kept as the visual baseline: two stacked sheets, west/interior cells left
% as open stubs, the east column tied by the existing per-site physical
% wrap loops.  It depicts Tr over the east column's PHYSICAL pairs — the
% right loops in the wrong index species.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

Nearest existing spelling (east wrap loops are per-site physical traces,
not the figure's inter-sheet virtual closure):
\[
  \begin{tenkzplanes}[rows=4, cols=4, open={(1-4,1-3)}, trace={(1-4,4)}]
  \end{tenkzplanes}
\]
\end{document}
