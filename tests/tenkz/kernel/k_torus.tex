%% Kernel fixture: LANGUAGE-1.0.md 12.2 (rmp-iii-a-torus-one), verbatim.
%% The picture below is the contract's printed sketch character for character,
%% with only the display-math delimiters removed; the kernel probe extracts the
%% sketch and diffs it against this body, so neither side may drift.
%% Expected: lattice sugar -> rows wire,wire,wire cols 3; four trace sides ->
%% wrap wires; one closed string whose basis winding class reaches the native
%% torus projection.  Two wound cycles crossing each other are
%% regression/r_wind_two_cycles.tex.
\documentclass{standalone}
\usepackage{tenkz}
\makeatletter
\ExplSyntaxOn
\file_input:n { tenkz-kernel.code.tex }
\ExplSyntaxOff
\makeatother
\begin{document}
\tenkzkernel
\begin{tenkz}[lattice={3x3},          % sugar: rows={wire,wire,wire}, cols=3
              west=trace, east=trace, north=trace, south=trace]
  \tnwire[kind=string, species=flux, closed,
          wind={1,0}]                   % the homotopy class is recorded
\end{tenkz}
\end{document}
