% Regression: route=orth squares a closed ring's turns, and every orth route
% states its `string' record (LionSR/tenkz, K2 review of 2026-08-29).
%
% `route=orth' was accepted by the language for any string but consulted only
% on the open branch, so a closed ring drew the smooth Hobby cycle whatever
% the author asked for.  The author panel behind
% rmp-iii-a-g-injective-projector draws its projector ring as a square; the
% kernel could not say that.
%
% The kernel also built the orth path by hand, so it skipped the string
% engine's registration and its `string' event: an orth route was drawn and
% never recorded, though docs/tenkz/TNLOG.md declares `string' for every
% declared curve.  Both directions are pinned below -- the closed route must
% square, and both routes must speak.
\documentclass{standalone}
\usepackage{tenkz}
\begin{document}
\tenkzkernel
\begin{tenkz}[rows={wire,wire,wire}, cols=3, bonds=none]
  \tn[skin=box, at=(1,2), name=n]{}
  \tn[skin=box, at=(2,3), name=e]{}
  \tn[skin=box, at=(3,2), name=s]{}
  \tn[skin=box, at=(2,1), name=w]{}
  \tnwire[kind=string, name=square, closed, route=orth,
    via={(1,1), n, (1,3), e, (3,3), s, (3,1), w}]
\end{tenkz}
\begin{tenkz}[rows={wire,wire,wire}, cols=3, bonds=none]
  \tn[skin=box, at=(2,2), name=c]{}
  \tnwire[kind=string, name=step, route=orth, via={(1,3)}]{(1,1)}{(3,3)}
\end{tenkz}
\end{document}
