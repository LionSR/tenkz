%% Regression: a later frame replaces the earlier frame and its basis.
\documentclass{standalone}
\usepackage{tenkz}
\begin{document}
\tenkzkernel
\begin{tenkz}[rows={wire}, cols=1, bonds=none,
              frame={flat,basis={wire at (0,0),wire at (1,0)}},
              frame=circle]
  \tn[at=(1,1)]{Y}
\end{tenkz}
\end{document}
