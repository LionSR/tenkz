%% Negative regression: an invalid frame cannot install a basis in recovery.
\documentclass{standalone}
\usepackage{tenkz}
\begin{document}
\tenkzkernel
\begin{tenkz}[rows={wire}, cols=1, bonds=none,
              frame={bogus,basis={wire at (0,0)}}]\end{tenkz}
\end{document}
