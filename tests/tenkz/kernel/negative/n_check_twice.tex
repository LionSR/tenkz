% Negative regression: one relation carries one waiver and one reason.  Two
% opt-outs for one relation record the comparison as waived twice, and a
% reader counting records against relations finds an equation that does not
% add up.
\documentclass{standalone}
\usepackage{tenkz}
\begin{document}
\tenkzkernel
\begin{tenkzeq}[check={signature, off={1: first}, off={01: second}}]
  \begin{tenkz}[rows={wire}, cols=1, bonds=none, west=open]
    \tn[ports={180:virtual}]{A}
  \end{tenkz}
  =
  \begin{tenkz}[rows={wire}, cols=1, bonds=none, west=open]
    \tn[ports={180:virtual}]{B}
  \end{tenkz}
\end{tenkzeq}
\end{document}
