% Negative regression: the audit states the audit it runs, the equivalence it
% is taken up to, and its recorded opt-outs.  A word outside that list is
% something the author believes the audit will do and it will not, so it is
% refused rather than ignored.
\documentclass{standalone}
\usepackage{tenkz}
\begin{document}
\tenkzkernel
\begin{tenkzeq}[check={signature, modulo=member}]
  \begin{tenkz}[rows={wire}, cols=1, bonds=none, west=open]
    \tn[ports={180:virtual}]{A}
  \end{tenkz}
  =
  \begin{tenkz}[rows={wire}, cols=1, bonds=none, west=open]
    \tn[ports={180:virtual}]{B}
  \end{tenkz}
\end{tenkzeq}
\end{document}
