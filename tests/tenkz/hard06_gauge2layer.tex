% Regression: hard06_gauge2layer — imported current-behavior fixture from the handoff corpus.
% Formula: O (a box on a doubled wire) = X^{-1} [A over conj(A), physical bond shared]
% T06 — RMP sec2fig6-1: two-layer bra-ket object.
% O (a box on a doubled wire) = X^{-1} [A over conj(A), physical bond shared]
% X, with X^{-1} and X sitting on the DOUBLED (both-layer) wire.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) grid attempt — the sandwich exists, but an on-wire matrix spanning BOTH
layer wires does not; \verb|\tnX| lives on one row's wire only:
\[
  \begin{tenkz}[rows={wire:fused}]
    \tnX{O}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[sandwich]
    \tnX{X^{-1}} & \tn{A} & \tnX{X}\\
                 & \tn*{A} &
  \end{tenkz}
\]

(b) grid attempt with \verb|\tnfuse| to collapse the two layers before the X
(fused stub dangles; X cannot sit on it):
\[
  \begin{tenkz}[sandwich]
    \tnfuse{} & \tn{A} & \tnfuse[combined=east]{}\\
              & \tn*{A} &
  \end{tenkz}
\]

(c) free-tier attempt — raw coordinates; ring = on-wire matrix skin:
\[
  \begin{tenkzfree}
    \tnput[ring]{o}{(0,0)}{O}
    \tnjoin[fused]{o.west}{-0.6,0}
    \tnjoin[fused]{o.east}{0.6,0}
  \end{tenkzfree}
  \;=\;
  \begin{tenkzfree}
    \tnput[dot]{a}{(0,0.45)}{A}
    \tnput[dot]{ab}{(0,-0.45)}{\overline{A}}
    \tnjoin{a.south}{ab.north}
    \tnput[ring]{xi}{(-0.9,0)}{X^{-1}}
    \tnput[ring]{x}{(0.9,0)}{X}
    \tnjoin[route=curve, out=90, in=180]{xi.north}{a.west}
    \tnjoin[route=curve, out=270, in=180]{xi.south}{ab.west}
    \tnjoin[route=curve, out=0, in=90]{a.east}{x.north}
    \tnjoin[route=curve, out=0, in=270]{ab.east}{x.south}
    \tnjoin[fused]{xi.west}{-1.55,0}
    \tnjoin[fused]{x.east}{1.55,0}
  \end{tenkzfree}
\]
\end{document}
