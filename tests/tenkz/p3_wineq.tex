% Regression: p3_wineq — imported current-behavior fixture from the handoff corpus.
% Formula: O_{W1} |psi>  =  O_{W2} |psi>  =  O_{W3} |psi>  =  O_{W4} |psi>,
% p3_wineq — arXiv:1804.04964 (normal PEPS FT), Corollary 8 sketch,
% p. 24 (middle figure).
% Comparing consecutive sliding windows needs only a (2L+1) x (2K+1)
% clip; the paper draws the comparison on an OPEN 3x4 patch: the four
% window insertions around the fixed edge agree,
%   O_{W1} |psi>  =  O_{W2} |psi>  =  O_{W3} |psi>  =  O_{W4} |psi>,
%   W1 = (1-2, 3-4),  W2 = (1-2, 2-3),  W3 = (1-2, 1-2),
%   W4 = (2-3, 1-2),  e = <(2,2),(2,3)> fixed, open boundaries
%   (dangling boundary legs on every rim site).
%
% Hue mapping as in p3_slidecd: window -> slot=selected outline, green
% edge -> distinguished edge; both Action-hued, contrast by line weight.
\documentclass[varwidth=32cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
$
\vcenter{\hbox{%
\begin{tenkzlattice}[rows=3, cols=4, boundary legs, compact]
  \tnregion[slot=selected, outline]{(1-2,3-4)}
  \tnedge{(2,2)-(2,3)}
\end{tenkzlattice}}}
\;=\;
\vcenter{\hbox{%
\begin{tenkzlattice}[rows=3, cols=4, boundary legs, compact]
  \tnregion[slot=selected, outline]{(1-2,2-3)}
  \tnedge{(2,2)-(2,3)}
\end{tenkzlattice}}}
\;=\;
\vcenter{\hbox{%
\begin{tenkzlattice}[rows=3, cols=4, boundary legs, compact]
  \tnregion[slot=selected, outline]{(1-2,1-2)}
  \tnedge{(2,2)-(2,3)}
\end{tenkzlattice}}}
\;=\;
\vcenter{\hbox{%
\begin{tenkzlattice}[rows=3, cols=4, boundary legs, compact]
  \tnregion[slot=selected, outline]{(2-3,1-2)}
  \tnedge{(2,2)-(2,3)}
\end{tenkzlattice}}}
$
\end{document}
