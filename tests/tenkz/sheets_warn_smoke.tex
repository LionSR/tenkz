% Regression: sheets_warn_smoke — exercises lattice pictures with `rows=2, cols=2, sheets={ket, gate, bra}…
% Formula: geometry-only; no tensor identity.
\documentclass[border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=2, cols=2, sheets={ket, gate, bra},
                     frame=oblique, open={(1,1,2)}]
\end{tenkzlattice}
\end{document}
