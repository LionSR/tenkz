% Regression: sheets_warn_smoke — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
\documentclass[border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=2, cols=2, sheets={ket, gate, bra},
                     frame=oblique, open={(1,1,2)}]
\end{tenkzlattice}
\end{document}
