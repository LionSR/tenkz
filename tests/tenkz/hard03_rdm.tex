% Regression: hard03_rdm — exercises T03 — reduced density matrix of n sites (RMP sec2fig8a): rho^R / rho^L…
% Formula: geometry-only; no tensor identity.
% T03 — reduced density matrix of n sites (RMP sec2fig8a):
% rho^R / rho^L boundary dots sitting ON the bent bonds that close the
% ket and bra layers into each other at both ends.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) grid attempt — open sandwich; the boundary closure through the rho dots
cannot be said (periodic closes each row to itself, not ket into bra):
\[
  \begin{tenkz}[rows={ket,bra}]
    \tn{} & \tn{} & \tndots & \tn{}\\
    \tn*{} & \tn*{} & \tndots & \tn*{}
  \end{tenkz}
\]

(a2) grid attempt with rows={bra,bra}: both layers' legs open and pointing
down, matching the RMP orientation; the boundary closure is still absent:
\[
  \begin{tenkz}[rows={bra,bra}]
    \tn{} & \tn{} & \tndots & \tn{}\\
    \tn*{} & \tn*{} & \tndots & \tn*{}
  \end{tenkz}
\]

(b) grid attempt with periodic (wrong topology: two disjoint traces):
\[
  \begin{tenkz}[rows={ket,bra}, periodic]
    \tn{} & \tn{} & \tndots & \tn{}\\
    \tn*{} & \tn*{} & \tndots & \tn*{}
  \end{tenkz}
\]

(c) free-tier attempt — raw coordinates everywhere:
\[
  \begin{tenkzfree}
    \tnput[dot]{k1}{(0,0.4)}{}   \tnput[dot]{k2}{(0.9,0.4)}{}
    \tnput[dot]{k3}{(2.7,0.4)}{}
    \tnput[dot]{b1}{(0,-0.4)}{}  \tnput[dot]{b2}{(0.9,-0.4)}{}
    \tnput[dot]{b3}{(2.7,-0.4)}{}
    \tnjoin{k1.east}{k2.west} \tnjoin{b1.east}{b2.west}
    \tnjoin{k3.west}{1.5,0.4} \tnjoin{b3.west}{1.5,-0.4}
    \tnput[dot]{rR}{(-0.75,0)}{\rho^R}
    \tnput[dot]{rL}{(3.45,0)}{\rho^L}
    \tnjoin[route=curve, out=180, in=90]{k1.west}{rR.north}
    \tnjoin[route=curve, out=270, in=180]{rR.south}{b1.west}
    \tnjoin[route=curve, out=0, in=90]{k3.east}{rL.north}
    \tnjoin[route=curve, out=90, in=0]{b3.east}{rL.south}
    \tnjoin{k1.south}{0,0.05} \tnjoin{k2.south}{0.9,0.05}
    \tnjoin{k3.south}{2.7,0.05}
    \tnjoin{b1.south}{0,-0.75} \tnjoin{b2.south}{0.9,-0.75}
    \tnjoin{b3.south}{2.7,-0.75}
  \end{tenkzfree}
\]
(ellipsis between the second and third columns omitted: no dots token in the free tier)
\end{document}
\end{document}
