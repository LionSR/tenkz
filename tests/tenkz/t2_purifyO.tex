% Regression: t2_purifyO — imported current-behavior fixture from the handoff corpus.
% t2_purifyO — RMP arXiv:2011.12127, Fig. sec2fig6, file
% fig2_fig2-pepe_sec2fig6-1.pdf: the local-purification sufficient
% condition for MPDO positivity (the Stinespring-flavoured picture:
% the mixed-state tensor factors through a purification).
%
% Formula:  O^{s s'}_{alpha beta}
%    = sum  (X^{-1})_{alpha,(a b)} A^{s}_{a a'} conj(A)^{s'}_{b b'}
%           X_{(a' b'), beta},
% i.e.  O = (X^{-1} (x) 1)(A (x) conj A)(X (x) 1)  with the purification
% ancilla index of A contracted against conj(A): O is the 4-leg MPDO
% tensor (virtual west/east, physical up/down), A the 4-leg purification
% tensor, X the 3-leg gauge transform fusing the doubled virtual space
% (a b) into O's single virtual index.
%
% Declarative reading (RHS):
%   rows={op, op}          — A's down leg pairs onto conj(A)'s up leg:
%                            the straight vertical wire IS the ancilla
%                            contraction; A's up and conj(A)'s down legs
%                            stay open (O's physical pair);
%   \tnX[wires=2, combined=west] — X^{-1}: the tall gauge capsule
%                            spanning both layers, label inscribed; its
%                            single fused stub (entering at the
%                            capsule's vertical centre) is O's west
%                            virtual index; mirrored east for X.
%   \tn*{A}                — the conjugated lower-layer tensor.
% Signatures match across `=`: (virtual-west 1, virtual-east 1,
% physical-up 1, physical-down 1) on both sides.
%
% RESOLVED (0.6): the former noted gap — the reference's tall rounded
% capsule with the label inside, single outer wire at the capsule's
% centre — is now stated by `combined=west|east` on the wires=2
% capsule (the \tnfuse consumed-boundary pattern lifted to tall
% glyphs); the earlier \tnfuse-bar spelling drew a bar with the label
% beside its stub.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: the 4-leg MPDO tensor O
  \begin{tenkz}[physical=updown, tensor style=box]
    \tn{O}
  \end{tenkz}
  \;=\;
  % RHS: X^{-1} capsule | A over conj(A) | X capsule
  \begin{tenkz}[rows={op, op}, tensor style=box]
    \tnX[wires=2, combined=west]{X^{-1}} & \tn{A} &
      \tnX[wires=2, combined=east]{X}\\
    & \tn*{A} &
  \end{tenkz}
\]
\end{document}
