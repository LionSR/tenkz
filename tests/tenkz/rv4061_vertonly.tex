% Regression: rv4061_vertonly — exercises grid pictures with `frame=vertical, rows={wire}` through \tn.
% Formula: geometry-only; no tensor identity.
\documentclass{article}\usepackage{tenkz}\begin{document}
X1\begin{tenkz}[frame=vertical, rows={wire}] \tn{A} & \tn{B} \end{tenkz}X2
\end{document}
