% Regression: p3_probe_pitch2 — imported current-behavior fixture from the handoff corpus.
% Formula: p3 probe v2 — does the metric-profile crash (pitch=/compact/inline on a
% p3 probe v2 — does the metric-profile crash (pitch=/compact/inline on a
% grid picture containing \tn) persist in the current surface?
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[physical=up, pitch=18mm]
    \tn{A}
  \end{tenkz}
  \quad
  \begin{tenkz}[physical=up, compact]
    \tn{A}
  \end{tenkz}
\]
\end{document}
