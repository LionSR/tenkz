% Regression: hard03_v2 — imported current-behavior fixture from the handoff corpus.
% hard03 v2 — n-site reduced density matrix of a uniform MPS (RMP sec2fig8).
%
% Formula:
%   [rho_n]_{(s_1..s_n),(s'_1..s'_n)}
%     = Tr[ rho^R  A^{s_1} ... A^{s_n}  rho^L  (A^{s'_1} ... A^{s'_n})^dagger ]
% Reference topology: a racetrack of virtual bonds — ket rail on top (legs UP,
% open), bra rail below (legs DOWN, open), the two rails closed into each
% other at both ends by bends carrying the fixed-point matrices rho^R (west)
% and rho^L (east).
%
% Declarative status with the current grammar:
%   EXPRESSIBLE — ket layer with open UP legs over bra layer with open DOWN
%   legs and NO inter-layer contraction, via rows={ket,wire,bra} with an
%   empty wire row (a ket row pairs only onto a bra/op row directly below,
%   so the interposed empty wire layer suppresses the automatic pairing);
%   the fixed-point matrices rho^R / rho^L as on-wire matrices (\tnX) on the
%   rails (on a wire their position is topologically free, so on-rail
%   placement carries the same contraction as on-bend placement);
%   the four open virtual stubs (boundary=open default).
%   NOT EXPRESSIBLE — the two inter-layer bends that close ket-west into
%   bra-west and ket-east into bra-east: `boundary=periodic` closes each row
%   TO ITSELF (two disjoint traces, wrong topology) and no `west=cup` /
%   `east=cup` key exists.  The pairwise identification of the stubs is
%   therefore stated in words below the picture.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \rho_n \;=\;
  % ket rail: rho^R then A^{s_1} ... A^{s_n} (legs up, open);
  % empty wire row: suppresses ket->bra leg pairing (unpaired sandwich);
  % bra rail: conj(A^{s'_1}) ... conj(A^{s'_n}) then rho^L (legs down, open).
  \begin{tenkz}[rows={ket,wire,bra}, compact]
    \tnX{\rho^R} & \tn{} & \tn{} & \tndots & \tn{}\\
    \\
    & \tn*{} & \tn*{} & \tndots & \tn*{} & \tnX{\rho^L}
  \end{tenkz}
\]
(missing ink: the west stubs of the two rails are one bend, and likewise the
east stubs — the trace closure of the formula above)
\end{document}
