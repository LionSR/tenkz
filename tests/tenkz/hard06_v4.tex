% Regression: hard06_v4 — imported current-behavior fixture from the handoff corpus.
% T06 v4 — RMP arXiv:2011.12127, sec2fig6 (fig2-pepe_sec2fig6-1):
% local purification form of a positive translationally invariant MPO.
% Closes v3's known styling gap with `combined=` on the wires=2
% capsule (0.6): the paper's single outer wire enters the capsule at
% its vertical CENTRE (it is the fused index a), which v3 could only
% approximate by opening one row's boundary (`op:none` + align=2, the
% outer wire at the lower row's height).
%
% Formula:  O = X^{-1} [ sum_k A^k (x) conj(A^k) ] X,
% where A is the 4-leg purification tensor (physical s up, purification
% index k down onto conj(A), virtual a1/b1) and X is the gauge mapping
% the doubled virtual index (a1,a2) to O's single virtual index a.
%
% Declarative reading:
%   rows={op, op} — A over conj(A); the automatic op-op pairing is the
%   purification index k; s (up) and s' (down) stay open.
%   \tnX[wires=2, combined=west]{X^{-1}} / [.., combined=east]{X} —
%   ONE tall on-wire capsule spanning both layer wires, terminating
%   them at its edges, and presenting the single fused index a as a
%   centre-height stub on its outer side (the \tnfuse consumed-boundary
%   pattern lifted to tall glyphs): both rows' boundary stubs on that
%   side are consumed and the signature counts ONE virtual index there,
%   so both sides of `=` read (1,1,1,1) with no row-sealing keys.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: the 4-leg MPO tensor O (physical s up, s' down, virtual a/b)
  \begin{tenkz}[physical=updown, tensor style=box]
    \tn{O}
  \end{tenkz}
  \;=\;
  % RHS: X^{-1} [ A (x) conj(A) ] X — capsules span both layers, the
  % A–conj(A) bond is the purification index k, and the outer virtual
  % wires leave the capsules at their vertical centres.
  \begin{tenkz}[rows={op, op}, tensor style=box]
    \tnX[wires=2, combined=west]{X^{-1}} & \tn{A} &
      \tnX[wires=2, combined=east]{X}\\
                                         & \tn*{A} &
  \end{tenkz}
\]
\end{document}
