% Regression: gr_t4_lasso — renders (L^y_abx)^{z,ij}_{c,k mu} delta_{y,y'} I_y = [closed scalar diagram.
% Formula: (L^y_abx)^{z,ij}_{c,k mu} delta_{y,y'} I_y = [closed scalar diagram:
% Target 4 — arXiv:2203.12563 eq (12): the L-symbol closed "lasso" diagram
%   (L^y_abx)^{z,ij}_{c,k mu} delta_{y,y'} I_y = [closed scalar diagram:
%   staircase of gray action bars i, j, k; S-curved red wire b crossing
%   down into the black fusion bar mu; wire labels y', a, z, b, x, c, y]
% Free-layer attempt: boxes stand in for the (unavailable) bar glyphs,
% \tnjoin curves for the S-bend.  No wire hue, no below-glyph labels.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \left(L^{y}_{abx}\right)^{z,ij}_{c,k\mu}\,\delta_{y,y'}\,\mathbb{I}_y
  \;=\;
  \begin{tenkzfree}
    % gray staircase: i (left, tall), j (lower), k (right); mu = black bar
    \tnput[box]{i}{(0,0)}{}
    \tnput[box]{j}{(8mm,-4mm)}{}
    \tnput[box]{m}{(26mm,2mm)}{}
    \tnput[box]{k}{(33mm,-2mm)}{}
    % y' in, a over the top into mu
    \tnjoin[label={y'}]{-6mm,0mm}{i.west}
    \tnjoin[label=a]{i.north east}{m.north west}
    % z step down into j
    \tnjoin[label=z]{i.east}{j.west}
    % b: the S-curve from j down across into the bar mu
    \tnjoin[route=curve, out=0, in=180, label=b]{j.north east}{m.south west}
    % x underneath, c between mu and k, y out
    \tnjoin[label=x]{j.south east}{k.south west}
    \tnjoin[label=c]{m.east}{k.north west}
    \tnjoin[label=y]{k.east}{39mm,-2mm}
  \end{tenkzfree}
\]

\end{document}
