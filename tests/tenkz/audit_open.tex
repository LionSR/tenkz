% Regression: audit_open — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
% Audit: can call sites fake open virtual boundary legs today?
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\newcommand{\head}[1]{\par\medskip\noindent\textbf{#1}\par\smallskip}
\begin{document}

\head{A — transfer map with flanking ghosts (target: four open virtual legs)}
\[
  \mathcal{E}_A \;=\;
  \begin{tenkz}[sandwich]
    \tnghost{} & \tn{A} & \tnghost{} \\
    \tnghost{} & \tn*{A} & \tnghost{}
  \end{tenkz}
\]

\head{B — gauge equation with flanking ghosts}
\[
  \begin{tenkz}[physical=up]
    \tnghost{} & \tn[up=$i$]{B} & \tnghost{}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[physical=up]
    \tnghost{} & \tnX{X} & \tn[up=$i$]{A} & \tnX{X^{-1}} & \tnghost{}
  \end{tenkz}
\]

\head{C — blocking RHS with ghosts}
\[
  \begin{tenkz}[physical=up]
    \tnghost{} & \tn[pill, wide=2, up={$i_1\cdots i_L$}]{A^{(L)}} & \tnghost{}
  \end{tenkz}
\]

\head{D — ghost + periodic interaction (should NOT be used together)}
\begin{tenkz}[periodic, physical=up]
  \tnghost{} & \tn{A} & \tn{A} & \tnghost{}
\end{tenkz}

\head{E — ghost with brace span miscount check}
\[
  \begin{tenkz}[physical=up]
    \tnghost{} & \tn[up=$i_1$]{A}\tnspan[brace below]{4}{L\text{ copies}} &
    \tn[up=$i_2$]{A} & \tndots & \tn[up=$i_L$]{A} & \tnghost{}
  \end{tenkz}
\]

\end{document}
