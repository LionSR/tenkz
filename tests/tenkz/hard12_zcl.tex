% Regression: hard12_zcl — renders LEGS pairwise from above/below = M with a single loop. The.
% Formula: LEGS pairwise from above/below = M with a single loop.  The
% T12 — RMP ZCL-MPDO: M M with racetrack loops closing ADJACENT PHYSICAL
% LEGS pairwise from above/below = M with a single loop.  The
% physical-leg-trace genre (spec key `trace=physical`, unimplemented).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) grid attempt — the closest implemented key is `periodic`, which traces
the VIRTUAL wire, not the physical legs:
\[
  \begin{tenkz}[physical=updown, tensor style=box, periodic]
    \tn[mpo]{M} & \tn[mpo]{M}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[physical=updown, tensor style=box, periodic]
    \tn[mpo]{M}
  \end{tenkz}
\]

(b) free-tier attempt — raw coordinates, arcs by hand:
\[
  \begin{tenkzfree}
    \tnput[mpo]{m1}{(0,0)}{M} \tnput[mpo]{m2}{(1.1,0)}{M}
    \tnjoin{m1.east}{m2.west}
    \tnjoin{m1.west}{-0.55,0} \tnjoin{m2.east}{1.65,0}
    \tnjoin{m1.north}{0,0.35} \tnjoin{m2.north}{1.1,0.35}
    \tnjoin[route=curve, out=90, in=90]{0,0.35}{1.1,0.35}
    \tnjoin{m1.south}{0,-0.35} \tnjoin{m2.south}{1.1,-0.35}
    \tnjoin[route=curve, out=270, in=270]{0,-0.35}{1.1,-0.35}
  \end{tenkzfree}
  \;=\;
  \begin{tenkzfree}
    \tnput[mpo]{m}{(0,0)}{M}
    \tnjoin{m.west}{-0.55,0} \tnjoin{m.east}{0.55,0}
    \tnjoin{m.north}{0,0.35} \tnjoin{m.south}{0,-0.35}
    \tnjoin[route=curve, out=90, in=0]{0,0.35}{0.45,0.6}
    \tnjoin[route=curve, out=270, in=0]{0,-0.35}{0.45,-0.6}
  \end{tenkzfree}
\]
(the RHS single-M loop should connect its up leg to its down leg around the
right side of the box — an around-the-glyph arc with no primitive)
\end{document}
