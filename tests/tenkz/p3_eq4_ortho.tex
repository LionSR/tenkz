% Regression: p3_eq4_ortho — renders W_{ab}^{c,mu} . What_{ab}^{d,nu} = delta_{c,d} delta_{mu,nu} 1_{chi_c}.
% Formula: W_{ab}^{c,mu} . What_{ab}^{d,nu} = delta_{c,d} delta_{mu,nu} 1_{chi_c}
% P3 — arXiv:2203.12563 eq (4): fusion-tensor orthogonality
%   W_{ab}^{c,mu} . What_{ab}^{d,nu} = delta_{c,d} delta_{mu,nu} 1_{chi_c}
% Paper ink: fused wire c enters a fat bar from the west, the pair wires
% a (top) and b (bottom) connect it to the mirror bar, fused wire d leaves
% east; mu and nu on top of the bars.
%
% tenkz spelling: two adjacent \tnfuse bars, combined legs facing OUTWARD
% (the supported orientation) — the bars' pair rows join automatically, and
% each combined stub is the picture's one open virtual index per side, so
% the boundary signature reads (west 1 | east 1), the signature of the
% identity matrix on the c block.
% Label gaps: the combined stubs (c, d) have no label slot (west/east
% label need a row stub, which the bars consume); `bond label` is
% row-1-only, so the pair wire b cannot be labelled.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={wire:operator, wire:operator},
                bond label={$a$ at 1-2}]
    \tnfuse[combined=west]{\mu} & \tnfuse[combined=east]{\nu} \\
    &
  \end{tenkz}
  \;=\; \delta_{c,d}\,\delta_{\mu,\nu}\,\mathbb{1}_{c}
\]

\end{document}
