% Regression: t2_pepo5 — renders O = sum_{s, s'} C( prod_r M_r^{s_r s'_r} ) |s_1..s_N><s'_1..s'_N|.
% t2_pepo5 — RMP arXiv:2011.12127, fig2-pepe_sec2fig5PEPO: a PEPO on a
% 4 x 4 window of the square lattice.
%
% Formula: O = sum_{s, s'} C( prod_r M_r^{s_r s'_r} ) |s_1..s_N><s'_1..s'_N|,
% one 6-index tensor per site: four virtual bonds to the lattice
% neighbours (bonds cut by the window dangle as boundary legs) plus TWO
% physical indices per site — an output (up) and an input (down), the
% operator's row and column indices.  The RMP draws each site as a bead
% with a vertical line THROUGH it: the up and the down physical leg.
%
% Declarative reading: oblique lattice window with boundary legs and
% physical=updown (0.6) — every site carries the operator's row index
% up AND its column index down, the vertical line through the bead of
% the RMP's drawing.
%
% RESOLVED (0.6): formerly STILL-BLOCKED on the key
% /tenkz/lattice/physical/updown (the choice stopped at up|none, so
% this file could only draw a PEPS); acceptance probe
% t2_probe_pepoupdown.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  O \;=\;
  \begin{tenkzlattice}[rows=4, cols=4, frame=oblique,
                       physical=updown, boundary legs]
  \end{tenkzlattice}
\]
\end{document}
