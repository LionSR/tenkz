% Regression: p3_erase — imported current-behavior fixture from the handoff corpus.
% Formula: O_{W1} |psi'>  =  O_{W4} |psi'>   on   P \ E,
% p3_erase — arXiv:1804.04964 (normal PEPS FT), Corollary 8 sketch,
% p. 24 (bottom figure).
% Corner erasure: add the two-site corner tensors at the upper left and
% lower right of a 3x4 clip and invert the resulting regions; the
% corners are erased, and on the surviving staircase the first and last
% window insertions agree:
%   O_{W1} |psi'>  =  O_{W4} |psi'>   on   P \ E,
%   P = the 3x4 clip,  E = {(1,1),(1,2)} u {(3,3),(3,4)},
%   W1 = (1-2, 3-4)  (upper-right block),
%   W4 = (2-3, 1-2)  (lower-left block),
%   e  = <(2,2),(2,3)> fixed.
%
% Divergence noted: the paper's erasure leaves half-bond stubs on the
% surviving neighbours — the virtual indices OPENED by inverting the
% corner tensors; \tnsite[removed] deletes the site together with its
% bonds and draws no cut stubs, so the rendered patch under-reports its
% open virtual indices (no `removed stubs`-like key on \tnsite).
\documentclass[varwidth=26cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
$
\vcenter{\hbox{%
\begin{tenkzlattice}[rows=3, cols=4, boundary legs, compact]
  \tnsite[removed]{(1,1)} \tnsite[removed]{(1,2)}
  \tnsite[removed]{(3,3)} \tnsite[removed]{(3,4)}
  \tnregion[slot=selected, outline]{(1-2,3-4)}
  \tnedge{(2,2)-(2,3)}
\end{tenkzlattice}}}
\;=\;
\vcenter{\hbox{%
\begin{tenkzlattice}[rows=3, cols=4, boundary legs, compact]
  \tnsite[removed]{(1,1)} \tnsite[removed]{(1,2)}
  \tnsite[removed]{(3,3)} \tnsite[removed]{(3,4)}
  \tnregion[slot=selected, outline]{(2-3,1-2)}
  \tnedge{(2,2)-(2,3)}
\end{tenkzlattice}}}
$
\end{document}
