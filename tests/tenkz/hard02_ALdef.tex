% Regression: hard02_ALdef — imported current-behavior fixture from the handoff corpus.
% Formula: [A_L^s]_{alpha,beta} = right-pointing triangle, west stub alpha, east stub beta,
% T02 — triangle tensor definition (RMP AL/AR):
% [A_L^s]_{alpha,beta} = right-pointing triangle, west stub alpha, east stub beta,
% physical leg s below.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

(a) grid attempt — a lone cell; are boundary virtual stubs drawn at all?
\[
  [A_L^s]_{\alpha,\beta} \;=\;
  \begin{tenkz}[physical=down]
    \tn[down=$s$]{A_L}
  \end{tenkz}
\]

(b) grid attempt with ghost end cells to force wire segments as fake stubs:
\[
  [A_L^s]_{\alpha,\beta} \;=\;
  \begin{tenkz}[physical=down, bond label={$\alpha$ at 1-2}]
    \tnghost{} & \tn[down=$s$]{A_L} & \tnghost{}
  \end{tenkz}
\]

(c) free-tier attempt — raw coordinates, box standing in for the triangle:
\[
  [A_L^s]_{\alpha,\beta} \;=\;
  \begin{tenkzfree}
    \tnput[box]{t}{(0,0)}{A_L}
    \tnjoin[label=\alpha]{t.west}{-0.55,0}
    \tnjoin[label=\beta]{t.east}{0.55,0}
    \tnjoin{t.south}{0,-0.45}
  \end{tenkzfree}
\]
\end{document}
