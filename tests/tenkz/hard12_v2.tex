% Regression: hard12_v2 — renders with E := tr_phys(M) = sum_s M^{ss}.
% Formula: with E := tr_phys(M) = sum_s M^{ss}
% T12 v2 — RMP ZCL-MPDO.  Formula: with E := tr_phys(M) = sum_s M^{ss}
% (the MPDO tensor M with its own up (bra) and down (ket) physical legs
% closed by a trace), the zero-correlation-length condition reads
%     E . E  =  E        (the physically-traced tensor is idempotent).
% Reference ink: each M carries a dotted oval from its up-leg tip, around
% the east side of the glyph (crossing the virtual wire), to its own
% down-leg tip; the virtual wire stays open at both ends of each side.
%
% Declarative near-miss: `physical=updown` states the up/down physical
% pair per tensor and `boundary=open` the open virtual ends, but no
% implemented key closes a cell's up leg with its own down leg — the
% spec key `trace=physical` (close physical legs by trace) is
% unimplemented, and the around-the-glyph loop routing it needs has no
% primitive.  The legs below are left open; the trace ink is missing.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % LHS: E . E — two traced tensors sharing one virtual bond
  \begin{tenkz}[physical=updown, tensor style=box]
    \tn[mpo]{M} & \tn[mpo]{M}
  \end{tenkz}
  \;=\;
  % RHS: E — one traced tensor
  \begin{tenkz}[physical=updown, tensor style=box]
    \tn[mpo]{M}
  \end{tenkz}
\]
\end{document}
