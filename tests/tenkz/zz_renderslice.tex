% Regression: zz_renderslice — fraction-placed atoms through the production
% geometry/render path, plus the normalized WIRE contract.
% Formula: |psi> = A B C; A spans two columns, pinning half-pitch placement.
\documentclass[varwidth=10cm, border=6pt]{standalone}
\usepackage{tenkz}
\tnset{species={alpha}}
\begin{document}
\ExplSyntaxOn
\int_new:N \g__tenkz_test_picture_int
\cs_new_eq:NN \__tenkz_test_derive_bonds: \tenkz_model_derive_bonds:
\cs_set_protected:Npn \tenkz_model_derive_bonds:
  {
    \int_gincr:N \g__tenkz_test_picture_int
    \__tenkz_test_derive_bonds:
    \__tenkz_model_count:nN {wire} \l_tmpa_int
    \int_compare:nNnTF {\g__tenkz_test_picture_int} = {1}
      {
        \int_compare:nNnF {\l_tmpa_int} = {2}
          {
            \PackageError{tenkz-test}
              {Expected~two~model~WIRE~records;~got~\int_use:N\l_tmpa_int}
              {Grid~ownership~must~be~resolved~before~bond~derivation.}
          }
        \int_zero:N \l_tmpb_int
        \__tenkz_model_map_ids:nn {wire}
          {
            \int_incr:N \l_tmpb_int
            \__tenkz_model_get:nnN {##1}{dir} \l_tmpa_tl
            \str_if_eq:VnF \l_tmpa_tl {from}
              {
                \PackageError{tenkz-test}
                  {Expected~WIRE~direction~'from';~got~'\l_tmpa_tl'}
                  {WIRE~records~must~preserve~the~row~direction.}
              }
            \__tenkz_model_get:nnN {##1}{style} \l_tmpa_tl
            \str_if_eq:eeF { \l_tmpa_tl } {species~alpha~bond}
              {
                \PackageError{tenkz-test}
                  {Expected~species~WIRE~style;~got~'\l_tmpa_tl'}
                  {WIRE~records~must~preserve~the~normalized~row~style.}
              }
            \__tenkz_model_get:nnN {##1}{role} \l_tmpa_tl
            \str_if_eq:eeF { \l_tmpa_tl } {operator}
              {
                \PackageError{tenkz-test}
                  {Expected~WIRE~role~'operator';~got~'\l_tmpa_tl'}
                  {WIRE~records~must~preserve~the~resolved~row~role.}
              }
            \__tenkz_model_get:nnN {##1}{species} \l_tmpa_tl
            \str_if_eq:eeF { \l_tmpa_tl } {alpha}
              {
                \PackageError{tenkz-test}
                  {Expected~WIRE~species~'alpha';~got~'\l_tmpa_tl'}
                  {WIRE~records~must~preserve~the~resolved~row~species.}
              }
            \__tenkz_model_get:nnN {##1}{name} \l_tmpa_tl
            \tl_set:Ne \l_tmpb_tl
              {
                \int_compare:nNnTF {\l_tmpb_int} = {1}
                  {bond-2-3}{bond-3-4}
              }
            \tl_if_eq:NNF \l_tmpa_tl \l_tmpb_tl
              {
                \PackageError{tenkz-test}
                  {Expected~WIRE~name~'\l_tmpb_tl';~got~'\l_tmpa_tl'}
                  {Row-1~bonds~must~name~their~MARK~targets.}
              }
          }
      }
      {
        \int_compare:nNnF {\l_tmpa_int} = {0}
          {
            \PackageError{tenkz-test}
              {Expected~no~WIREs~for~an~unsupported~combined~attachment}
              {A~centred~multi-row~face~must~not~be~duplicated~per~row.}
          }
      }
  }
\ExplSyntaxOff
\ExplSyntaxOn
% A containing scope may itself own geometry.  The picture must discard this
% resolved placement before reusing the same frame and placement names.
\__tenkz_geom_frame_declare_rotation:nnnn {gridflat}{0}{100}{100}
\__tenkz_geom_place_cell:nnnn {cell:1-1} {gridflat} {0} {0}
\__tenkz_geom_resolve:
\ExplSyntaxOff
\begin{tenkz}[
  bond dir=right,
  bond label={D at 2-3},
  rows={ket:operator:species=alpha:dir left},
]
  \tn[wide=2]{A} & & \tn{B} & \tn{C}
\end{tenkz}
\begin{tenkz}[rows={ket,ket}]
  \tn[box,wires=2,combined=east]{X} & \tn[box,wires=2]{Y}\\
  &
\end{tenkz}
\begin{tenkz}[rows={ket,ket}]
  \tnfuse[combined=east]{F} & \tn[box,wires=2]{Y}\\
  &
\end{tenkz}
\end{document}
