% Regression: t2_lassodef — imported current-behavior fixture from the handoff corpus.
% Formula: e (lassos of Figboundary-theoryc):
% t2_lassodef — RMP arXiv:2011.12127, fig2-pepe_Figboundary-theorya-1:
% the polar decompositions of the two lasso tensors.
%
% Formulae (lassos of Figboundary-theoryc):
%   A^{I}      = U^{A}_{I} sigma^{A} :  the region-R lasso splits into an
%     isometry U^A (carrying the physical bundle I) times the positive
%     boundary matrix sigma^A on the \partial R wire;
%   B^{\bar I} = sigma^{B} U^{B}_{\bar I} :  mirrored for the complement.
%
% Declarative rendering, one 1-row grid per side of each equation:
%   * eq 1: west=none (the lasso wire starts at the tensor), physical=up;
%     LHS one bead; RHS the box U^A_I (inscribed label, up leg) then the
%     legless bead sigma^A on the wire, east stub open = \partial R.
%   * eq 2: the mirror image, east=none, sigma^B before the box U^B_Ibar.
% Boundary signatures should be: one physical-up index, one virtual stub
% (east for eq 1, west for eq 2) — the lasso's other side has NO index.
%
% STILL-BLOCKED on per-side boundary policy: the keys /tenkz/grid/west
% and /tenkz/grid/east (west=none / east=none) PARSE in the current
% build but are never consulted by the renderer or the signature writer
% (\l__tenkz_westpol_tl / \l__tenkz_eastpol_tl are set and cleared, never
% read), so every picture below still draws a spurious stub on the sealed
% side and logs virtual-west=1|virtual-east=1.  boundary=none is no
% substitute here: each side of each equality is ASYMMETRIC (one end
% sealed, the other an open \partial R stub).  The keys stay in the
% source so the file becomes the acceptance test once the policy is
% wired.
%
% Also noted: the RMP distinguishes the A-side (red) from the B-side
% (black) by hue; tenkz has no per-atom colour key, so both equations
% render in ink monochrome.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[physical=up, west=none]
    \tn{}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[physical=up, west=none]
    \tn[box]{U^{A}_{I}} & \tn[no legs, label pos=north]{\sigma^{A}}
  \end{tenkz}
\]
\[
  \begin{tenkz}[physical=up, east=none]
    \tn{}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[physical=up, east=none]
    \tn[no legs, label pos=south]{\sigma^{B}} & \tn[box]{U^{B}_{\bar{I}}}
  \end{tenkz}
\]
\end{document}
