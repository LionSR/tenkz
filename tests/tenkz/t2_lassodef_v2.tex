% Regression: t2_lassodef_v2 — imported current-behavior fixture from the handoff corpus.
% Formula: t2_lassodef: that file recorded STILL-BLOCKED because west= / east=
% t2_lassodef_v2 — RMP arXiv:2011.12127, file fig2_fig2-pepe_newfig12 (the
% polar decompositions of the two lasso tensors).  Re-attempt of
% t2_lassodef: that file recorded STILL-BLOCKED because west= / east=
% parsed but were never consulted; the renderer now reads the per-side
% policy (tenkz-grid.code.tex, the westpol/eastpol branch near the stub
% pass), so this file is the acceptance re-audit on the same ink.
%
% Formulae:
%   A^{I}      = U^{A}_{I} sigma^{A} :  the region-R lasso splits into an
%     isometry U^A_I (carrying the physical bundle I) times the positive
%     boundary matrix sigma^A riding the \partial R wire;
%   B^{\bar I} = sigma^{B} U^{B}_{\bar I} :  mirrored for the complement.
%
% Ink, eq 1: LHS one bead, up leg, west side SEALED (the lasso wire only
% leaves eastward); RHS box U^A_I (up leg) bonded to the legless bead
% sigma^A, east stub open = \partial R.  Eq 2 is the mirror (east sealed,
% sigma^B before the box, west stub open).
% Expected boundary signatures: eq 1 both sides virtual-west=0,
% virtual-east=1, physical-up=1; eq 2 mirrored — the .tnlog check.
%
% Noted gap: the RMP separates the A side (red) from the B side
% (black/gray) by hue; no per-atom colour key, monochrome here.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % eq 1: A^I = U^A_I sigma^A — west sealed on both sides
  \begin{tenkz}[physical=up, west=none]
    \tn{}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[physical=up, west=none]
    \tn[box]{U^{A}_{I}} & \tn[no legs, label pos=north]{\sigma^{A}}
  \end{tenkz}
\]
\[
  % eq 2: B^Ibar = sigma^B U^B_Ibar — east sealed on both sides
  \begin{tenkz}[physical=up, east=none]
    \tn{}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[physical=up, east=none]
    \tn[no legs, label pos=south]{\sigma^{B}} & \tn[box]{U^{B}_{\bar{I}}}
  \end{tenkz}
\]
\end{document}
