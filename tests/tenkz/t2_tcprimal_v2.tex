% Regression: t2_tcprimal_v2 — imported current-behavior fixture from the handoff corpus.
% Formula: Gauss law  g_1 g_2 g_3^{-1} g_4^{-1} = e.  The PEPS tensor blocks one
% RMP 2011.12127, appendix "The Toric Code and quantum double models",
% eq:app:tcode-rep-primal (ref_ fig_app_tc-primal.pdf).  Corrects
% t2_tcprimal.tex, which drew the DUAL figure's corner-to-corner cross:
% the primal tensor's interior is the blocked-plaquette DIAMOND.
%
%   Quantum double for a finite group G: spins |g>, g in G, live on the
%   EDGES of the oriented square lattice; the ground state is the
%   equal-weight superposition of the configurations obeying the vertex
%   Gauss law  g_1 g_2 g_3^{-1} g_4^{-1} = e.  The PEPS tensor blocks one
%   plaquette (aligned diagonally w.r.t. the lattice): the inner diamond
%   is the plaquette, its four edges carry the physical edge spins
%   g_1 (NW), g_2 (NE), g_3 (SE), g_4 (SW) -- squiggle marks in the
%   paper -- and the four corner legs carry the virtual Gauss-law
%   products
%       g_1 g_4^{-1} (west),   g_1 g_2 (depth-up),
%       g_2^{-1} g_3 (east),   g_4 g_3 (depth-down)
%   (the nonzero configurations of eq:app:tcode-rep-primal).
%
% Declarative model: a 3x3 oblique sheet; the shaded blocked tensor is a
% filled region over all cells; corners + centre removed, so the only
% surviving lattice ink is the four mid-side boundary legs (the virtual
% corner indices, sheared with the sheet); the plaquette is the diamond
% cycle of distinguished edges between the mid-side sites.
%
% Honest gaps: tenkzlattice has no leg/edge label key, so the eight group
% labels cannot be attached; the physical squiggle marks on the diamond
% edges have no primitive.
\documentclass[border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=3, cols=3, frame=oblique, site=none, boundary legs]
  % the blocked-plaquette tensor (shaded sheet)
  \tnregion[slot=selected]{(1-3,1-3)}
  % keep only the four mid-side sites: their boundary legs are the four
  % virtual indices g_1 g_4^{-1}, g_1 g_2, g_2^{-1} g_3, g_4 g_3
  \tnsite[removed]{(1,1)} \tnsite[removed]{(1,3)}
  \tnsite[removed]{(3,1)} \tnsite[removed]{(3,3)}
  \tnsite[removed]{(2,2)}
  % the blocked plaquette: physical edge spins g_1..g_4 on the diamond
  \tnedge{(2,1)-(1,2)} \tnedge{(1,2)-(2,3)}
  \tnedge{(2,3)-(3,2)} \tnedge{(3,2)-(2,1)}
\end{tenkzlattice}
\end{document}
