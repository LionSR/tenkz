% Regression: p3_eq08 — imported current-behavior fixture from the handoff corpus.
% Formula: = sum_{i,y} V^i (A on the fused wire y, physical leg up) W^i
% p3 — arXiv:2203.12563 eq (8): the action-tensor decomposition
%   (MPO tensor on wire a) acting on (MPS tensor on wire x)
%     = sum_{i,y} V^i (A on the fused wire y, physical leg up) W^i
% LHS: op row (operator hue, red) over ket row; the automatic physical
%   pairing is the contracted index between the beads; ghost cells extend
%   the virtual wires.  Only ONE `east label' exists per picture (it
%   attaches to the first open row), so the paper's x label is missing.
% RHS: the fused mid-wire site (a one-row word between a fusing bar and a
%   splitting bar) has no grid form — the manual tracks the splitting
%   glyph as a missing primitive — so three stitched pictures approximate
%   it and the wires break at the picture seams.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={op:operator, ket}, east label={$a$}]
    \tnghost{} & \tn{} & \tnghost{} \\
    \tnghost{} & \tn{} & \tnghost{}
  \end{tenkz}
  \;=\; \sum_{i,y}\;
  \begin{tenkz}[rows={wire:operator, wire}, west label={$a$}]
    \tnghost{} & \tnfuse[span=2, combined=east]{i} \\
    \tnghost{} &
  \end{tenkz}
  \!
  % NB: two workarounds are themselves blocked: \tnghost cells to
  % lengthen this wire crash in a single ket row (p3_ghostket_probe.tex),
  % and pitch=/compact/inline crash any grid picture (p3_pitch_probe.tex),
  % so the fused y wire stays at its default one-site width.
  \begin{tenkz}[physical=up, west label={$y$}, east label={$y$}]
    \tn{}
  \end{tenkz}
  \!
  \begin{tenkz}[rows={wire:operator, wire}, east label={$a$}]
    \tnfuse[span=2, combined=west]{i} & \tnghost{} \\
    & \tnghost{}
  \end{tenkz}
\]

\end{document}
