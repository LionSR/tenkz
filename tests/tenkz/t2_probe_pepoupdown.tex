% Regression: t2_probe_pepoupdown — imported current-behavior fixture from the handoff corpus.
% t2_probe_pepoupdown — key probe for the t2_pepo5 blocker (RMP
% arXiv:2011.12127, file fig2_fig2-pepe_sec2fig5PEPO: the PEPO window,
% one ket (up) AND one bra (down) physical leg per lattice site).
%
% Formula: O = sum_{s,s'} C( prod_r M_r^{s_r s'_r} ) |s><s'| — the site
% tensor is 6-legged: four virtual bonds plus the operator's row (up) and
% column (down) physical indices.
%
% This file INTENTIONALLY asks for the missing key:
%   /tenkz/lattice/physical/updown
% Expected outcome under the current build: pgfkeys rejects the choice
% value `updown' (the lattice family stops at physical=up|none), so the
% compile FAILS — the failure is the probe's measurement.  When the key
% lands, this file becomes the acceptance test: a 4x4 oblique window whose
% every site carries both physical legs, boundary bonds open.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  O \;=\;
  \begin{tenkzlattice}[rows=4, cols=4, frame=oblique, boundary legs,
                       physical=updown]
  \end{tenkzlattice}
\]
\end{document}
