% Regression: p3_eq09 — renders the bars = delta_{y,z} delta_{i,j} I_y.
% Formula: the bars  =  delta_{y,z} delta_{i,j} I_y
% p3 — arXiv:2203.12563 eq (9): orthogonality of the action tensors
%   (splitting bar i)(fusing bar j) bubble, red a over black x between
%   the bars  =  delta_{y,z} delta_{i,j} I_y
% Grid form: \tnfuse[combined=west] facing \tnfuse[combined=east] in one
%   picture; the row-1 bond between them takes the operator (red) hue,
%   the row-2 bond stays black.
% Missing keys, noted in the verdict: boundary labels on the fused stubs
%   (the paper's y and z), a row selector on `bond label' (x on row 2 —
%   the key hardcodes row 1), and the bar labels i, j render above the
%   combined stubs where the paper sets them under the bars.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={wire:operator, wire}, bond label={$a$ at 1-2}]
    \tnfuse[span=2, combined=west]{i} & \tnfuse[span=2, combined=east]{j} \\
    &
  \end{tenkz}
  \;=\; \delta_{y,z}\,\delta_{i,j}\,\mathbb{I}_y
\]

\end{document}
