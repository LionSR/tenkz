% Regression: hard04_v2 — renders [rho_n]_{(s),(s')} = Tr[ rho^R A^{s_1}..A^{s_n} rho^L (A^{s'_1}..A^{s'_n})^dagger ].
% Formula: [rho_n]_{(s),(s')} = Tr[ rho^R A^{s_1}..A^{s_n} rho^L (A^{s'_1}..A^{s'_n})^dagger ]
% hard04 v2 — spectrum of the n-site reduced density matrix (RMP fig9-1):
%
%   eig( rho_n ),   rho_n as in sec2fig8:
%   [rho_n]_{(s),(s')} = Tr[ rho^R A^{s_1}..A^{s_n} rho^L (A^{s'_1}..A^{s'_n})^dagger ]
%
% Reference: the same racetrack object (ket rail legs up, bra rail legs
% down, rho^R / rho^L on the end bends) inside stretchy math parentheses —
% this also exercises the slice contract: the picture's wire axis must sit
% on the math axis so \left( ... \right) centres correctly.
%
% Declarative status: identical to hard03 — the unpaired two-layer chain,
% the on-rail rho^R / rho^L, the open stubs, and the math-axis baseline are
% expressible; the two inter-layer end bends are not (`west=cup` /
% `east=cup` missing; `periodic` traces each row to itself).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \mathrm{eig}\left(
  % same declarative core as hard03_v2; the end bends closing the rails
  % into each other are the still-missing ink.
  \begin{tenkz}[rows={ket,wire,bra}, compact]
    \tnX{\rho^R} & \tn{} & \tn{} & \tndots & \tn{}\\
    \\
    & \tn*{} & \tn*{} & \tndots & \tn*{} & \tnX{\rho^L}
  \end{tenkz}
  \right)
\]
\end{document}
