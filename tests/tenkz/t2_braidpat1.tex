% Regression: t2_braidpat1 — imported current-behavior fixture from the handoff corpus.
% Target: RMP 2011.12127 fig3_fig3-pepe_Braidpat1.pdf (half-braiding of
% anyonic strings on the PEPS virtual level).
% Formula: the R-move — a red anyon string (endpoints a, i as blue
% on-bond / on-site tensors) half-braids around a green MPO string
% (endpoints b, j), passing OVER the lattice bonds and looping around
% the green string between the two oblique lattice rows.
% Declarative attempt: an oblique PEPS fragment (tenkzlattice,
% frame=oblique, physical legs up) with the crossed plaquette diagonal
% marked as a distinguished edge — the only string-like statement the
% lattice tier admits today.
% Honest gaps / blockers (still-blocked): \tnsite has no hue/species
% slot, so the green MPO sites and blue anyon endpoints (i, j, a, b —
% the figure's cast) cannot be stated; there is no off-lattice string
% primitive (free curve over/under the bonds with crossing gaps), so the
% half-braid loop — the point of the figure — is inexpressible; \tnedge
% only marks existing lattice bonds.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkzlattice}[rows=2, cols=4, frame=oblique, physical=up]
    % nearest expressible trace of the braid path: the crossed diagonal
    \tnedge{(2,2)-(1,3)}
  \end{tenkzlattice}
\]

\end{document}
