% Regression: p3_ghostket_probe — renders p3 probe — minimal reproduction: \tnghost in a physical=up (ket) row.
% Formula: p3 probe — minimal reproduction: \tnghost in a physical=up (ket) row
% p3 probe — minimal reproduction: \tnghost in a physical=up (ket) row
% crashes with "Runaway argument? \tenkz_leg_vec:nn ... Paragraph ended
% before \tikz@cc@parse@factor was complete."  The same pattern in a
% sandwich (two-row) picture compiles (p3_eq32 LHS), so the defect is the
% single-ket-row ghost leg path.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[physical=up]
    \tnghost{} & \tn{} & \tnghost{}
  \end{tenkz}
\]
\end{document}
