% Regression: hard03_v3 — imported current-behavior fixture from the handoff corpus.
% hard03 v3 — n-site reduced density matrix of a uniform MPS (RMP sec2fig8),
% re-attempted with the upgraded keys: `west=cup` / `east=cup` with
% on-bend fixed-point matrices and the `nopair` rows modifier.
%
% Formula:
%   [rho_n]_{(s_1..s_n),(s'_1..s'_n)}
%     = Tr[ rho^R  A^{s_1} ... A^{s_n}  rho^L  (A^{s'_1} ... A^{s'_n})^dagger ]
%
% Reference topology (four explicit sites, as drawn in the RMP): a racetrack —
% ket rail on top with OPEN up legs, bra rail below with OPEN down legs, the
% two rails closed into each other at both ends by cups carrying the
% fixed-point matrices rho^R (west bend) and rho^L (east bend).
%
% Key mapping:
%   rows={ket:nopair, bra}   — nopair keeps all physical legs open (rho_n is
%                              a matrix on the n-site physical space);
%   west={cup=$\rho^R$}    — west cup with rho^R riding the bend apex;
%   east={cup=$\rho^L$}    — east cup with rho^L riding the bend apex.
%
% Signature: (vw 0, ve 0, pu 4, pd 4) — all virtual indices closed by the
% cups, one open physical index per site per layer.  The LHS of the display
% is algebra (rho_n), so there is no picture-to-picture balance to check.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \rho_n \;=\;
  % ket rail: A^{s_1} .. A^{s_4}, up legs open; bra rail: conjugates, down
  % legs open; the cups + on-bend dots are the trace through rho^R / rho^L.
  \begin{tenkz}[rows={ket:nopair, bra},
                west={cup=$\rho^R$}, east={cup=$\rho^L$}]
    \tn{} & \tn{} & \tn{} & \tn{}\\
    \tn*{} & \tn*{} & \tn*{} & \tn*{}
  \end{tenkz}
\]
\end{document}
