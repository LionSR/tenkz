% Regression: gr_t3_action — imported current-behavior fixture from the handoff corpus.
% Formula: (MPO tensor acting on MPS tensor) = sum_{i,y} V (A on y) Vhat
% Target 3 — arXiv:2203.12563 eq (8): action-tensor decomposition
%   (MPO tensor acting on MPS tensor) = sum_{i,y} V (A on y) Vhat
% Mixed wire types in one vertex: the gray action bar V merges a red MPO
% wire (a) and a black MPS wire (x) into one black wire (y).
% LHS: clean grid (op:operator over ket).  RHS: grid attempt with \tnfuse
% across rows of different hue; the y wire is a stub between rows, so the
% middle A dot with its physical leg cannot sit on it.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={op:operator, ket}]
    \tnghost{} & \tn{} & \tnghost{} \\
    \tnghost{} & \tn{} & \tnghost{}
  \end{tenkz}
  \;=\; \sum_{i,y}\;
  % the action bars are gray in the paper: a scoped style append recolors
  % EVERY fusion bar in the picture (works only because both bars here are
  % gray; eq (10)-(12) mix black and gray bars in one picture)
  {\tikzset{fusion map/.append style={draw=black!45, fill=black!45}}%
  \begin{tenkz}[rows={wire:operator, wire}]
    \tnghost{} & \tnfuse[combined=east]{} &
      \tnfuse[combined=west]{} & \tnghost{} \\
    \tnghost{} & & & \tnghost{}
  \end{tenkz}}
\]

\end{document}
