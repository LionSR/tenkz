% Regression: cd_edge — renders charge-less fusion subtrees alone and inside a grid diagram.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=20cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
Charge-less inner: \tntree{((a\,b)\,c)_e}

Bare pair (no root charge): \tntree{(a\,b)}

Deep charge-less: \tntree{(((a\,b)\,c)\,d)}

Grid mode:
\begin{tenkzcd}
  A \arrow{r} & B \arrow{d} \\
  \tntree{(a\,b)_c} \arrow{r} & C
\end{tenkzcd}
\end{document}
