% Regression: p3_eq4_v2 — imported current-behavior fixture from the handoff corpus.
% Formula: W_{ab}^{c,mu} . What_{ab}^{d,nu} = delta_{c,d} delta_{mu,nu} 1_{chi_c} .
% P3 v2 — arXiv:2203.12563 eq (4): fusion-tensor orthogonality,
%   W_{ab}^{c,mu} . What_{ab}^{d,nu} = delta_{c,d} delta_{mu,nu} 1_{chi_c} .
% Paper ink: fused wire c enters a fat bar from the west, the pair wires
% a (top) and b (bottom) run to the mirror bar, fused wire d leaves east;
% mu and nu atop the bars.
%
% tenkz spelling: two adjacent \tnfuse bars with combined legs facing
% OUTWARD; the pair rows bond bar-to-bar automatically, each combined stub
% is the picture's one open virtual index per side — the boundary
% signature of an identity matrix on the c block.  The row-1 pair wire
% carries its block label via `bond label`.
% Noted gaps: the combined stubs have no label slot (west/east label
% attaches only to a ROW stub, which fuse bars suppress), so the paper's
% c and d wire labels survive only in the delta; `bond label` is
% row-1-only, so the b pair wire is unlabeled.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={wire:operator, wire:operator},
                bond label={$a$ at 1-2}]
    \tnfuse[combined=west]{\mu} & \tnfuse[combined=east]{\nu} \\
    &
  \end{tenkz}
  \;=\; \delta_{c,d}\,\delta_{\mu,\nu}\,\mathbb{I}_{c}
\]

\end{document}
