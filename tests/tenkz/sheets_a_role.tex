% Regression: sheets_a_role — byte-compares role-list sheets={ket,bra} against numeric sheets=2 with pairing.
% Formula: strict byte-compare of sheets={ket,bra} + outer legs=none against the
% sheets_a_role — the role-list spelling of xyz_a_lattice.tex, for the
% strict byte-compare of sheets={ket,bra} + outer legs=none against the
% numeric sheets=2 + pairing picture (acceptance (a) of sheets_test).
\documentclass[border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=3, cols=3, sheets={ket, bra}, outer legs=none,
                     frame=oblique, plane slant=0.40, plane rise=0.45]
\end{tenkzlattice}
\end{document}
