% Regression: p3_multibond — imported current-behavior fixture from the handoff corpus.
% p3_multibond — arXiv:1804.04964, Lemma lem:inj_equal_tensors_2, source
% lines 1067-1092: two injective tensors joined by SEVERAL parallel bonds
% (the PEPS multi-edge pair), with an insertion X on one bond and a vertical
% ellipsis standing for the remaining bonds.
%
% Formula: for all X on all edges,
%          A_1 --(X on bond 1, bonds 2..k)-- A_2  =  B_1 --(same)-- B_2
%          ==> A_1 = lambda B_1, A_2 = lambda^{-1} B_2.
%   ink:  rows={ket,wire,wire} gives one physical row and two extra parallel
%         virtual rows; each tensor is a wires=3 glyph spanning all three
%         (a bead cannot span wires, so the skin is a box — the one rendering
%         deviation from the paper's dots).  X rides the TOP bond as an
%         on-wire \tnX; \tnghost routes the lower bonds straight through its
%         column.  boundary=none seals the outer ends: every virtual index
%         of the pair is contracted with its partner.
%   gap 1: the paper's \vdots BETWEEN the lower bonds (k arbitrary) has no
%         tenkz spelling — \tndots follows the chain axis (renders \cdots
%         along a horizontal row), so a "more parallel bonds" marker between
%         wire rows is missing; this reproduction fixes k = 3 explicit bonds.
%   gap 2: the per-side keys west=none / east=none parse but are consulted by
%         no draw pass (grid code sets \l__tenkz_westpol_tl and never reads
%         it), so sealing must be the all-sides boundary=none.
%   defect: \tn[wires=3]{A_1} WITHOUT an explicit skin double-labels — the
%         node pass upgrades dot -> box and inscribes the label, then the
%         dot-label pass re-resolves the skin from the recorded options
%         (still `dot`) and places a second, external label.  Worked around
%         here by the explicit `box` key.
% Signature balance: both sides vw 0, ve 0, pu 1 per tensor (pu 2 total).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

Multi-bond pair (1804.04964 lines 1069--1090): equal for all $X$ on all edges
\[
  \begin{tenkz}[rows={ket,wire,wire}, boundary=none]
    \tn[wires=3, box]{A_1} & \tnX{X} & \tn[wires=3, box]{A_2} \\
                           & \tnghost{} & \\
                           & \tnghost{} &
  \end{tenkz}
  \;=\;
  \begin{tenkz}[rows={ket,wire,wire}, boundary=none]
    \tn[wires=3, box]{B_1} & \tnX{X} & \tn[wires=3, box]{B_2} \\
                           & \tnghost{} & \\
                           & \tnghost{} &
  \end{tenkz}
\]
\end{document}
