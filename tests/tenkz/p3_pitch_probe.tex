% Regression: p3_pitch_probe — imported current-behavior fixture from the handoff corpus.
% Formula: keys [pitch=18mm], [compact], [inline], and with a document-level
% p3 probe — minimal reproduction: ANY metric-profile change crashes a
% grid picture containing \tn.  Fails identically with the picture-level
% keys [pitch=18mm], [compact], [inline], and with a document-level
% \tnset{pitch=18mm} wrapped around a default picture:
%   "Missing number, treated as zero. <to be read again> ("
%   then "Illegal unit of measure" and "No shape named 0".
% Default pitch (no key) compiles fine, so the metric system's
% document-level knobs are unusable in the current surface.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[physical=up, pitch=18mm]
    \tn{A}
  \end{tenkz}
\]
\end{document}
