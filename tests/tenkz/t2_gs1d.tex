% Regression: t2_gs1d — renders |psi(X)> = sum_{s_1..s_N} tr( X A^{s_1} A^{s_2} ... A^{s_N} ).
% t2_gs1d — RMP arXiv:2011.12127, fig4-pepe_GS1D: the MPS whose virtual
% loop closes through a boundary matrix X.
%
% Formula: |psi(X)> = sum_{s_1..s_N} tr( X A^{s_1} A^{s_2} ... A^{s_N} )
%          |s_1 ... s_N>.
%
% Declarative rendering: rows={ket, wire} + west=cup + east=cup.  The
% ket row is the word A A A ... A (physical legs up, open); the wire row
% is the bare return wire carrying the on-wire matrix X (\tnX); the two
% cups tie the word's virtual ends into the return wire, so the closed
% loop IS the trace and X rides it exactly where the RMP puts it (below
% the word, right of centre).  Boundary signature: N physical-up indices
% open, no virtual index open — a ket, as the formula demands.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  |\psi(X)\rangle \;=\;
  \begin{tenkz}[rows={ket, wire}, west=cup, east=cup, tensor style=box]
    \tn{A} & \tn{A} & \tn{A} & \tndots & \tn{A}\\
    \tnghost{} & \tnghost{} & \tnghost{} & \tnX{X} & \tnghost{}
  \end{tenkz}
\]
\end{document}
