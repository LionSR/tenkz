% Regression: t2_fusion — renders the associator on splitting trees with three unlabeled legs.
% Target: RMP 2011.12127 fig3_fusion.pdf (fusion-category recoupling).
% Formula: the associator on splitting trees with three unlabeled legs,
%     ((. .) .)  ~=  (. (. .)) ,   caption   ^1F ^1F = ^0F ^1F ^1F
% (a two-vertex splitting tree re-associated by an F-move; the paper
% draws root west / leaves east, red directed legs, gray disc vertices).
% House idiom: \tntree — leaves on the top line, root leg down.
% Honest gaps: tree edges carry no direction barbs, junctions render as
% house beads (no gray-disc vertex skin), orientation is the house
% vertical, and the wire hue is house ink, not the paper's red.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

% left: (a b) first, then with c   |   right: (b c) first, then under a
\[
  \tntree{(({}\,{})\,{})} \;\simeq\; \tntree{({}\,({}\,{}))}
\]
\[
  {}^{1}F\,{}^{1}F \;=\; {}^{0}F\,{}^{1}F\,{}^{1}F
\]

\end{document}
