% Regression: p3_probe_opop2 — imported current-behavior fixture from the handoff corpus.
% Formula: rows= list of the failed p3_min1 (crash: Missing number, treated as zero).
% p3 probe — TWO op rows BOTH carrying the `operator` modifier: the exact
% rows= list of the failed p3_min1 (crash: Missing number, treated as zero).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[rows={op:operator, op:operator}]
    \tn{T_a} \\
    \tn{T_b}
  \end{tenkz}
\]
\end{document}
