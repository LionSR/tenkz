% Regression: audit3 — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

% P9: stacked periodic, op row with LABELED up legs: does the label/leg
% collide with the racetrack?
P9:\quad
\begin{tenkz}[periodic, rows={op, bra}]
  \tn[mpo, up=$j_1$]{M} & \tn[mpo, up=$j_2$]{M} \\
  \tn*{A} & \tn*{A}
\end{tenkz}

\bigskip

% P10: same with dot skin
P10:\quad
\begin{tenkz}[periodic, rows={op, bra}]
  \tn[up=$j_1$]{M} & \tn[up=$j_2$]{M} \\
  \tn*{A} & \tn*{A}
\end{tenkz}

\end{document}
