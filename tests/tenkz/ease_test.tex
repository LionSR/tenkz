% Regression: ease_test — imported current-behavior fixture from the handoff corpus.
% ease_test.tex — fresh-eyes ease review of the tenkz double layer.
%
% Formula: rho_A = Tr_{c4} |psi><psi| for a 4x4 PEPS |psi>.  The double
% layer is ket over bra; the rightmost column's physical pairs close by
% trace loops (the traced-out sites), the other columns keep dangling
% stubs (the kept indices of the marginal).  The kept 2x2 corner A is
% marked as a region on the ket sheet, and one bra-sheet bond is drawn
% as a distinguished edge.
%
% Written from docs/tenkz/WHATS-NEW-0.5.md plus the header comments and
% key blocks of tenkz-lattice.code.tex only.
\documentclass[varwidth=20cm, border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}

% --- Figure 1: the marginal ---------------------------------------------
% rho_A = Tr_{c4} |psi><psi|.  tenkzplanes: ket = sheet 1 above bra =
% sheet 0.  open= / trace= address physical COLUMNS; values contain
% literal commas, so they are braced.  Kept columns 1-3 dangle, column 4
% is traced per site.
\begin{tenkzplanes}[rows=4, cols=4,
    open={(1-4,1-3)}, trace={(1-4,4)}]
  % (2) the kept 2x2 subsystem on the ket sheet: an unaddressed cell in
  % tenkzplanes lives on the KET sheet, so a sheetless region is a
  % ket-sheet region.
  \tnregion[outline, label={$A$}]{(1-2,1-2)}
  % (3) a distinguished edge on the bra sheet: (r,c,0) addresses bra.
  \tnedge{(3,2,0)-(3,3,0)}
\end{tenkzplanes}

\end{document}
