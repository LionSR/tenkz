% Regression: t2_mpuflow — imported current-behavior fixture from the handoff corpus.
% T2 mpuflow — RMP arXiv:2011.12127, fig. sec2fig9a: the normal form of
% a matrix-product unitary — index flow through the simple tensor.
%
% Formula: in the MPU normal form the site tensor factorizes so that the
% indices FLOW: the incoming physical index i_k leaves as (part of) the
% outgoing virtual index, and the incoming virtual index leaves as the
% outgoing physical index j_k,
%     U^{(i_k), (j_k)}_{alpha, beta} = delta_{j_k, alpha} delta_{i_k, beta}
% (schematically): inside each box the north wire BENDS EAST and the west
% wire BENDS SOUTH — the box is a wire permutation, not a vertex.
%
% Declarative status: the chain itself is a plain grid (four op-row boxes
% on one virtual wire, physical legs up and down), rendered below as the
% nearest approximation with the `mpo` skin (wires pass THROUGH the
% glyph).  The mathematical content, however, is the INTERNAL REROUTING
% (north->east, west->south), and no per-cell key states it.
%
% STILL-BLOCKED: missing primitive is a per-cell in-glyph rerouting skin,
% e.g. `reroute={north-east, west-south}`, drawing the two quarter-bends
% inside the box (the fig7 variant needs the same key twice, one per
% coloured wire species).  The approximation below has the right boundary
% signature (vw 1, ve 1, pu 4, pd 4) but states a vertex, not the
% permutation.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
Nearest declarative approximation (residual: boxes show crossing wires,
not the north\,$\to$\,east / west\,$\to$\,south bends):
\[
  \begin{tenkz}[physical=updown, tensor style=box]
    \tn[mpo]{} & \tn[mpo]{} & \tn[mpo]{} & \tn[mpo]{}
  \end{tenkz}
\]
\end{document}
