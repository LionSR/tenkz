% Regression: g06_stageC — exercises semantic roles on atoms, wires, joins, species styles, event fields, and…
% Formula: g06_stageC — the semantic role interface: role= on atoms, wires and
% g06_stageC — the semantic role interface: role= on atoms, wires and
% joins, declared species, style indirection, event fields, closure
% inheritance (the operator-hued traced row).
\documentclass[varwidth=16cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\tnset{species={right, left}}
\begin{document}

C1 role= on grid atoms (bead and inscribed skins):
\begin{tenkz}[physical=up]
  \tn[role=marked, up=$i$]{A} & \tn[up=$j$]{A} &
  \tn[box, role=operator, up=$k$]{O}
\end{tenkz}
\quad species on an atom:
\begin{tenkz}[physical=up]
  \tn[species=right, up=$i$]{R} & \tn[species=left, up=$j$]{L}
\end{tenkz}

\medskip
C2 row-scope species (the t2\_twoshift content):
\begin{tenkz}[rows={wire:species=right, wire:species=left}]
  \tn{A} & \tn{A} \\
  \tn{B} & \tn{B}
\end{tenkz}
\quad row-scope role + closure inheritance (operator ring, t2\_ared\_loop):
\begin{tenkz}[periodic, rows={op:operator}]
  \tn[up=$i$, down=$j$]{O} & \tn[up=$i'$, down=$j'$]{O}
\end{tenkz}
\quad hooks inherit too:
\begin{tenkz}[periodic, trace style=hooks, rows={op:operator}]
  \tn[up=$i$, down=$j$]{O} & \tn[up=$i'$, down=$j'$]{O}
\end{tenkz}

\medskip
C3 the operator-hued \verb|\tnjoin| (worklist item 1) and \verb|\tnput| roles:
\begin{tenkzfree}
  \tnput[ports={east:virtual}]{a}{(0,0)}{A}
  \tnput[role=marked, ports={west:virtual}]{b}{(1.0,0)}{B}
  \tnput[box, role=operator]{o}{(0.5,0.6)}{O}
  \tnjoin[role=operator]{a.east}{b.west}
  \tnjoin[species=right, route=vh]{a.north}{o.west}
\end{tenkzfree}

\medskip
C4 role on a gauge capsule and a species wire row:
\begin{tenkz}[rows={wire:species=left}]
  \tnX[role=passive]{X} & \tn{A} & \tnX{X^{-1}}
\end{tenkz}

\medskip
C5 lattice edge role (event carries the word):
\begin{tenkzlattice}[rows=3, cols=3]
  \tnedge[role=operator]{(2,1)-(2,2)}
  \tnedge[role=marked, label=$g$]{(1,2)-(2,2)}
\end{tenkzlattice}

\end{document}
