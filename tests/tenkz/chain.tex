% Regression: chain — expands a rectangular region range and draws the selected overlay on a 2-by-2 lattice.
% Formula: geometry-only; no tensor identity.
\documentclass[border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
% region cell-set chain probe, public spelling: the resolver expands the
% range term (1-2,1-2), the serializer feeds the tracer, the tracer draws
% the selected region over a 2x2 window
\begin{tenkzlattice}[rows=2, cols=2]
  \tnregion[slot=selected, label pos=north west, label=$R$]{(1-2,1-2)}
\end{tenkzlattice}
\end{document}
