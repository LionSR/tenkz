% Regression: t2_twoshift — imported current-behavior fixture from the handoff corpus.
% t2_twoshift — RMP arXiv:2011.12127, fig. sec2fig7c(b), file
% fig2_fig2-pepe_fig7.pdf: the index-zero MPU made of two shift
% operators, one right-moving and one left-moving.
%
% Formula: on the doubled local space C^2 (x) C^2 the MPU acts as
%     U = S_right (x) S_left,
% site tensor (schematically, one delta chain per species):
%     U^{(i i'),(j j')}_{(alpha alpha'),(beta beta')}
%       = delta_{i beta} delta_{j alpha}          (right-mover)
%         . delta_{i' alpha'} delta_{j' beta'}    (left-mover)
% — inside every site box the right-moving wire bends north->east and
% west->south, the left-moving wire bends north->west and east->south;
% the two species ride separate virtual rails (reference: blue = right,
% red = left) and never mix.  Index +1 - 1 = 0.
%
% Declarative status: the chain is three site boxes on TWO virtual
% rails with two up and two down physical legs per box, stated by the
% cell-level `physical=updown` on each wires=2 brick (one leg per
% spanned row and face, ordered west-to-east as the rails read
% top-to-bottom).
%
% STILL-BLOCKED: (1) per-cell in-glyph rerouting skin, e.g.
% reroute={north-east, west-south; north-west, east-south} — the same
% key t2_mpuflow.tex records for fig. sec2fig9a, needed here twice per
% box; (2) a per-wire hue key (the blue/right vs red/left species IS
% the figure's content; no colour is exposed).
% RESOLVED (0.6): physical legs on a wires=k brick — cell
% physical=updown places one up and one down leg per spanned row.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
Nearest declarative approximation (residual: no reroute bends, no wire
hues):
\[
  \begin{tenkz}[rows={wire,wire}]
    \tn[box, wires=2, physical=updown]{} &
    \tn[box, wires=2, physical=updown]{} &
    \tn[box, wires=2, physical=updown]{} \\
    & &
  \end{tenkz}
\]
\end{document}
