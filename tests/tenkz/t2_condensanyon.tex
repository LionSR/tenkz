% Regression: t2_condensanyon — renders |psi(a, abar)> — an anyon a at the lower-west site pair and its.
% Target: RMP 2011.12127 fig3_fig3-pepe_condensanyon.pdf (anyon pair on
% the PEPS double layer).
% Formula: |psi(a, abar)> — an anyon a at the lower-west site pair and its
% conjugate at the upper-east site pair (large blue dots on BOTH sheets),
% connected by TWO parallel red string operators (one per sheet) that run
% diagonally across the double layer, dressing the crossed bonds with
% small blue matrices.
% Declarative reading: tenkzplanes is the ket-over-bra oblique double layer.
% Per-sheet coordinates repeat the same marked path on sheet 1 (ket) and
% sheet 0 (bra), as the source repeats every segment and marker at y=+/-0.2.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkzplanes}[rows=6, cols=4, plane slant=0.30, sheet sep=0.25]
    \tnsite[role=marked, size=l]{(1,2,1)}
    \tnsite[role=marked, size=s]{(2,2,1)}
    \tnsite[role=marked, size=s]{(3,2,1)}
    \tnsite[role=marked, size=s]{(4,2,1)}
    \tnsite[role=marked, size=l]{(5,3,1)}
    \tnsite[role=marked, size=l]{(1,2,0)}
    \tnsite[role=marked, size=s]{(2,2,0)}
    \tnsite[role=marked, size=s]{(3,2,0)}
    \tnsite[role=marked, size=s]{(4,2,0)}
    \tnsite[role=marked, size=l]{(5,3,0)}
    \tnedge[role=operator]{(1,2,1)-(2,2,1)}
    \tnedge[role=operator]{(2,2,1)-(3,2,1)}
    \tnedge[role=operator]{(3,2,1)-(4,2,1)}
    \tnedge[role=operator]{(4,2,1)-(5,3,1)}
    \tnedge[role=operator]{(1,2,0)-(2,2,0)}
    \tnedge[role=operator]{(2,2,0)-(3,2,0)}
    \tnedge[role=operator]{(3,2,0)-(4,2,0)}
    \tnedge[role=operator]{(4,2,0)-(5,3,0)}
  \end{tenkzplanes}
\]

\end{document}
