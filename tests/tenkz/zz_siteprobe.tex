% Regression: zz_siteprobe — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=20cm, border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=3, cols=4, boundary legs]
  \tnsite[removed]{(2,2)}
  \tnsite[label=$x$]{(1,1)}
  \tnsite[role=marked]{(3,3)}
  \tnsite[role=operator, label=$O$]{(1,4)}
\end{tenkzlattice}
\end{document}
