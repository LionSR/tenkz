% Regression: zz_siteprobe — exercises lattice pictures with `rows=3, cols=4, boundary legs` through \tnsite.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=20cm, border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=3, cols=4, boundary legs]
  \tnsite[removed]{(2,2)}
  % Explicit medium and the omitted keys retain the historical bead and
  % north-east label placement.
  \tnsite[size=m, label=$x$]{(1,1)}
  \tnsite[size=s, role=marked]{(3,3)}
  \tnsite[size=l, role=operator, label=$O$, label pos=south west]{(1,4)}
\end{tenkzlattice}
\end{document}
