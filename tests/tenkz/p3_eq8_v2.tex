% Regression: p3_eq8_v2 — renders = sum_{y,i} Vhat_{ax}^{y,i} . A_y^{m} . V_{ax}^{y,i}.
% Formula: = sum_{y,i} Vhat_{ax}^{y,i} . A_y^{m} . V_{ax}^{y,i} .
% P3 v2 — arXiv:2203.12563 eq (8): action-tensor decomposition of the MPO
% acting on the MPS,
%   sum_l T_a^{m,l} A_x^{l}
%     = sum_{y,i} Vhat_{ax}^{y,i} . A_y^{m} . V_{ax}^{y,i} .
% Paper ink: red MPO wire a over black MPS wire x; the A dot's physical
% leg feeds the T dot's lower leg, T's upper leg stays open.  On the
% right the mixed chirality bars (one red pair leg, one black) around an
% A_y dot on the black fused wire; multiplicity i under each bar.
%
% tenkz spelling: op:operator over ket pairs the interface automatically
% and leaves exactly one open physical leg (up).  The bars are \tnfuse
% across ONE operator wire row and ONE plain wire row — the mixed hues
% carry the paper's V/W-versus-fusion-bar distinction; the bar pictures
% and the A_y picture are separate math atoms on the shared axis (the
% zipper splitting-glyph seam, as in eq 3).
% Label mapping: block labels ride the atoms (T_a, A_x, A_y); i is the
% \tnfuse bar label, drawn above the combined stub where the paper sets
% it below the bar.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \begin{tenkz}[rows={op:operator, ket}]
    \tn{T_a} \\
    \tn{A_x}
  \end{tenkz}
  \;=\; \sum_{y,i}\;
  \begin{tenkz}[rows={wire:operator, wire}]
    \tnghost{} & \tnfuse[combined=east]{i} \\
    \tnghost{} &
  \end{tenkz}
  \begin{tenkz}[rows={ket}]
    \tn{A_y}
  \end{tenkz}
  \begin{tenkz}[rows={wire:operator, wire}]
    \tnfuse[combined=west]{i} & \tnghost{} \\
    & \tnghost{}
  \end{tenkz}
\]

\end{document}
