% Regression: p3_eq8_action — imported current-behavior fixture from the handoff corpus.
% Formula: sum_l T_a^{m,l} A_x^l = sum_{y,i} Vhat_{ax}^{y,i} . A_y^m . V_{ax}^{y,i}
% P3 — arXiv:2203.12563 eq (8): the action-tensor decomposition
%   sum_l T_a^{m,l} A_x^l = sum_{y,i} Vhat_{ax}^{y,i} . A_y^m . V_{ax}^{y,i}
% Paper ink: red MPO wire a over black MPS wire x, T dot over A dot with
% one physical line through; RHS a chirality pair of GRAY bars merging the
% (a, x) pair into the single black wire y that carries A_y (up leg only);
% multiplicity i sits UNDER each bar.
%
% Attempt A (one picture): 3 rows — a (operator hue), y (ket, carries A_y),
%   x (plain wire); \tnfuse[span=3] puts the combined stub at the y row.
%   The gray skin is a scoped restyle of `fusion map` (all bars in the
%   picture recolor together; fine here, both bars are gray).
% Attempt B (three pictures): bars in their supported outward orientation,
%   the product spelled as juxtaposed atoms.
% Label gap: \tnfuse writes its label above the combined stub; the paper
% puts the action multiplicity i BELOW the bar.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

% --- Attempt A: single picture --------------------------------------
\[
  \begin{tenkz}[rows={op:operator, ket}]
    \tn{T_a} \\
    \tn{A_x}
  \end{tenkz}
  \;=\; \sum_{i,y}\;
  {\tikzset{fusion map/.append style={draw=black!55, fill=black!55}}%
  \begin{tenkz}[rows={wire:operator, ket, wire}]
    \tnghost{} & \tnfuse[span=3, combined=east]{i} & \tnskip &
      \tnfuse[span=3, combined=west]{i} & \tnghost{} \\
               &                         & \tn{A_y} &          & \\
    \tnghost{} &                         & \tnskip  &          & \tnghost{}
  \end{tenkz}}
\]

% --- Attempt B: the product as three juxtaposed atoms ----------------
\[
  \begin{tenkz}[rows={op:operator, ket}]
    \tn{T_a} \\
    \tn{A_x}
  \end{tenkz}
  \;=\; \sum_{i,y}\;
  {\tikzset{fusion map/.append style={draw=black!55, fill=black!55}}%
  \begin{tenkz}[rows={wire:operator, wire}]
    \tnghost{} & \tnfuse[combined=east]{i}
  \end{tenkz}
  \begin{tenkz}[rows={ket}]
    \tn{A_y}
  \end{tenkz}
  \begin{tenkz}[rows={wire:operator, wire}]
    \tnfuse[combined=west]{i} & \tnghost{}
  \end{tenkz}}
\]

\end{document}
