% Regression: p3_probe_opmod — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
% p3 probe — one op row with the `operator` wire-hue modifier: is the
% modifier itself the crash, or the op-over-op interface?
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[rows={op:operator}]
    \tn{T_a}
  \end{tenkz}
\]
\end{document}
