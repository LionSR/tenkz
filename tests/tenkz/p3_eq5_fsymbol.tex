% Regression: p3_eq5_fsymbol — imported current-behavior fixture from the handoff corpus.
% Formula: ((a b)_e c)_d = sum_{f,chi,eta} (F_{abc}^d)^{e mu nu}_{f chi eta} (a (b c)_f)_d
% P3 — arXiv:2203.12563 eq (5): the F-symbol between the two fusion trees
%   ((a b)_e c)_d = sum_{f,chi,eta} (F_{abc}^d)^{e mu nu}_{f chi eta} (a (b c)_f)_d
% Paper ink: stacked chirality bars; the canonical mathematical notation is
% the pair of fusion trees, which is \tntree's native object: leaves a,b,c
% on the top line, internal charge on the downward edge (e left / f right),
% total charge d on the root leg.
% Known gap: the junction multiplicity labels (mu, nu on the left tree's
% two vertices; chi, eta on the right tree's) have no slot in \tntree —
% they survive only inside the F coefficient.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

\[
  \tntree{((a\,b)_e\,c)_d}
  \;=\;
  \sum_{f,\chi,\eta}
  \left(F_{abc}^{d}\right)_{f\chi\eta}^{e\mu\nu}\,
  \tntree{(a\,(b\,c)_f)_d}
\]

\end{document}
