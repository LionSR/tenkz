% Regression: geom — imported current-behavior fixture from the handoff corpus.
% Formula: geometry-only; no tensor identity.
\documentclass[border=6pt]{standalone}
\usepackage{tenkz}
\makeatletter
\begin{document}
\ExplSyntaxOn
\int_new:N \g_pr_int  \int_gset:Nn \g_pr_int {2}
\dim_new:N \g_pm_dim  \dim_set:Nn \g_pm_dim {3pt}
\ExplSyntaxOff
\ExplSyntaxOn
H: \dim_eval:n { \tenkzl_cx:n {\g_pr_int} - \g_pm_dim } \par
I: \dim_eval:n { \tenkzl_cy:n {\g_pr_int} + \g_pm_dim } \par
\ExplSyntaxOff
\begin{tikzpicture}
\ExplSyntaxOn
\draw ( \dim_eval:n { \tenkzl_cx:n {\g_pr_int} - \g_pm_dim },
        \dim_eval:n { \tenkzl_cy:n {\g_pr_int} + \g_pm_dim } )
   -- ( \tenkzl_cx:n{1}, \tenkzl_cy:n{1} );
\ExplSyntaxOff
\end{tikzpicture}
done
\end{document}
