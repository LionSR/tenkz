% Regression: modes_test — renders group 1 under the default glyph policy (tensor style=dot), group 2 inside.
% Formula: group 1 under the default glyph policy (tensor style=dot), group 2 inside
% modes_test — the SAME suite of sources (modes_suite.inc) rendered twice:
% group 1 under the default glyph policy (tensor style=dot), group 2 inside
% a picture-group-scoped {\tnset{tensor style=box} ...}.  The sources are
% byte-identical between the groups: that is the point of the feature.
\documentclass[varwidth=16cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

Group 1 --- default policy (\texttt{tensor style=dot}):

\medskip
\input{modes_suite.inc}

\bigskip\hrule\bigskip

Group 2 --- \texttt{tensor style=box}:

\medskip
{\tnset{tensor style=box}%
\input{modes_suite.inc}}

\bigskip\hrule\bigskip

Per-picture override (document policy still dot; left picture opts in):
\[
  \begin{tenkz}[tensor style=box, sandwich]
    \tn{A} \\
    \tn*{A}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[sandwich]
    \tn{A} \\
    \tn*{A}
  \end{tenkz}
\]

\end{document}
