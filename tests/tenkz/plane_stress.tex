% Regression: plane_stress — exercises notch + ring (inset) + removed site + distinguished edge, all oblique.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=20cm, border=6pt]{standalone}
\usepackage{amsmath}
\usepackage{tenkz}
\begin{document}
% notch + ring (inset) + removed site + distinguished edge, all oblique
\begin{tenkzlattice}[rows=5, cols=5, frame=oblique, boundary legs]
  \tnregion[slot=selected,  name=R, label=$R$]{(2-4, 2-4)}
  \tnregion[slot=secondary, outline, inset=1, label=$S$, label at=south west]{R - (3,3)}
  \tnedge[distinguished]{(3,4)-(3,5)}
  \tnsite[removed]{(3,3)}
\end{tenkzlattice}

\medskip
% double layer: custom sheet sep, scattered open cells, warning path
\begin{tenkzplanes}[rows=3, cols=4, sheets={bra, ket}, sheet sep=2.2,
                    open={(1,2),(2-3,4)}]
\end{tenkzplanes}
\end{document}
