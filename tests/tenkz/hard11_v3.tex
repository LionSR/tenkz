% Regression: hard11_v3 — renders |psi> = ( prod_{layers l} (x)_k U^{(l)}_k ) |0>.
% T11 v3 — RMP sec2fig11b: state prepared from |0> by a widening
% tree/brick circuit of two-site gates (sequential generation).
% Previously BLOCKED on `wires=<k>`; now the gates ARE one-atom bricks.
%
% Formula:  |psi> = ( prod_{layers l} (x)_k U^{(l)}_k ) |0>,
% each U a gate spanning two adjacent wires; successive layers overlap
% brick-fashion and the wire count doubles per layer starting from the
% single |0> wire (depth reduced to 3 layers / 4 output wires here;
% the reference iterates the same doubling once more).
%
% Orientation: the grid's circuit convention is quantikz-like — wires
% are ROWS, time flows west -> east (the reference is the same picture
% rotated 90 degrees: |0> at the west, outputs east).
%
% Declarative reading:
%   \tn[box, wires=2]{} — an unlabeled brick TERMINATING both spanned
%   wires at its edges (quantikz \gate[2]); layer offsets rows 2-3,
%   then 1-2 / 3-4, are the brick pattern.
%   \tn[dot]{|0>} — the source bead on the trunk wire; its label takes
%   the free south quadrant (the reference's beside-the-dot label).
%   boundary=open — the four EAST stubs are the state's open indices.
% Known gap (see verdict): rows born mid-picture (at a gate or behind
% the |0> bead) still draw WEST stubs — the boundary policy has no
% per-side row control, so the four spurious west stubs cannot be
% sealed without also sealing the genuine east outputs.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % rows = wires; col 1: |0> source; col 2: first gate (wires 2-3);
  % col 3: second layer, brick-offset (wires 1-2 and 3-4)
  \begin{tenkz}[rows={wire,wire,wire,wire}]
     & & \tn[box, wires=2]{}\\
    \tn[dot]{|0\rangle} & \tn[box, wires=2]{} & \\
     & & \tn[box, wires=2]{}\\
     & &
  \end{tenkz}
\]
\end{document}
