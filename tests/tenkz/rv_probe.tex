% Regression: rv_probe — exercises Reviewer probe: box-mode cases modes_suite does not exercise. P1: S2-style…
% Formula: geometry-only; no tensor identity.
% Reviewer probe: box-mode cases modes_suite does not exercise.
% P1: S2-style MPO-MPO rows (T_g vs T_h) under box policy.
% P2: mixed-width labels in one row (A vs T_g vs A^{(2)}) — ragged-box check.
% P3: entanglement cut between boxes.
% P4: on-wire capsule (tnX) between boxes.
% P5: periodic stacked word with conjugate row, box policy.
\documentclass[varwidth=16cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\tnset{tensor style=box}
\begin{document}

P1:
\[
  \begin{tenkz}[rows={op:operator, op:operator}]
    \tn[mpo]{T_g} & \tndots & \tn[mpo]{T_g} \\
    \tn[mpo]{T_h} & \tndots & \tn[mpo]{T_h}
  \end{tenkz}
\]

P2:
\[
  \begin{tenkz}[physical=up]
    \tn{A} & \tn{T_g} & \tn{A^{(2)}} & \tn{B}
  \end{tenkz}
\]

P3:
\[
  \begin{tenkz}[physical=up]
    \tn{A} & \tn{A}\tncut{S_{[1,2]}} & \tn{A} & \tn{A}
  \end{tenkz}
\]

P4:
\[
  \begin{tenkz}[physical=up]
    \tnX{X} & \tn[up=$i$]{A} & \tnX{X^{-1}}
  \end{tenkz}
\]

P5:
\[
  \begin{tenkz}[periodic, rows={op, bra}]
    \tn[mpo]{M} & \tn[mpo]{M} & \tn[mpo]{M} \\
    \tn*{A} & \tn*{A} & \tn*{A}
  \end{tenkz}
\]

\end{document}
