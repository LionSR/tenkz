% Regression: t2_pairs — imported current-behavior fixture from the handoff corpus.
% Formula: |Psi> = ( (x)_{v in V} A[v] )  (x)_{e in E} |phi)_e ,
% RMP 2011.12127 Fig. 1 of Sec. II (label sec2fig1,
% ref_ fig2_fig2-pepe_sec2fig1.pdf): the projected entangled-pair
% construction of PEPS.
%
%   |Psi> = ( (x)_{v in V} A[v] )  (x)_{e in E} |phi)_e ,
%   |phi) = sum_{alpha=1}^{D} |alpha, alpha>   (maximally entangled pair
%   on every nearest-neighbour edge);  A[v] : (C^D)^{(x) z_v} -> C^d maps
%   the z_v ancillas at vertex v (the blob) to one physical spin.
%
% Declarative model: the oblique PEPS sheet with an on-site physical leg
% per vertex.  The paper draws each vertex as an ellipse blob enclosing
% one ancilla dot per incident bond, and the physical index as a detached
% red dot reached by the projection arrow A; the tenkz sheet states the
% same contraction with bead sites and straight physical legs.
\documentclass[border=6pt]{standalone}
\usepackage{tenkz}
\begin{document}
\begin{tenkzlattice}[rows=4, cols=4, frame=oblique, boundary legs,
                     physical=up]
\end{tenkzlattice}
\end{document}
