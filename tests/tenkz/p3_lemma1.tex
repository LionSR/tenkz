% Regression: p3_lemma1 — renders the equality of periodic three-site words with virtual insertions X and Y on bond…
% p3_lemma1 — arXiv:1804.04964, Lemma lem:inj_isomorph, source lines 254-275:
% a virtual insertion on a named bond of two trace words generating the
% same state.
%
% Formula: for every X on bond (1,2) there is Y with
%          sum tr{ A_1 X A_2 A_3 } |i_1 i_2 i_3>  =  sum tr{ B_1 Y B_2 B_3 } |...>
%   ink:  three-site periodic ket words; the deformation X (paper: red dot on
%         the bond between sites 1 and 2) is the on-wire matrix \tnX placed
%         BETWEEN the site columns — the named bond is the column interval it
%         occupies.  tenkz renders \tnX as an action-hued capsule, the library's
%         spelling of the paper's red bead.
% Signature balance: both sides vw 0, ve 0 (traced), pu 3.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

Lemma 1 (1804.04964 lines 256--273): insertion $X$ on bond $(1,2)$ has a partner $Y$
\[
  \begin{tenkz}[periodic, physical=up]
    \tn{A_1} & \tnX{X} & \tn{A_2} & \tn{A_3}
  \end{tenkz}
  \;=\;
  \begin{tenkz}[periodic, physical=up]
    \tn{B_1} & \tnX{Y} & \tn{B_2} & \tn{B_3}
  \end{tenkz}
\]
\end{document}
