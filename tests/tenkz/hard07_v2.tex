% Regression: hard07_v2 — renders |psi> = sum_{s_11..s_33} C( A^{s_11}_[l,r,u,d] ... A^{s_33} ).
% T07 v2 — RMP secfig4bPEPS (top panel): a finite PEPS window.
% Formula: |psi> = sum_{s_11..s_33} C( A^{s_11}_[l,r,u,d] ... A^{s_33} )
%          |s_11 ... s_33>,
% one 5-index tensor per site: 4 virtual bonds to lattice neighbours
% (bonds cut by the window dangle as boundary legs) and 1 open physical
% leg per site.  Declarative lattice genre: `boundary legs` for the cut
% virtual indices, `physical=up` for the on-site physical legs.
% The RMP draws the same object in an oblique (axonometric) projection so
% the physical legs leave the lattice plane visibly.  Verified outcome of
% the flat window: `physical=up` draws each interior site's leg COLLINEAR
% with (and shorter than) the vertical bond above it, and each top-row
% leg on top of the `boundary legs` up-stub — the physical index ink is
% unreadable, so the 5-index site tensor renders as 4-index.  No
% implemented lattice key angles the on-site leg away from the bonds.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkzlattice}[rows=3, cols=3, boundary legs, physical=up]
  \end{tenkzlattice}
\]
\end{document}
