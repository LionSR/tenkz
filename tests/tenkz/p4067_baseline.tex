% Regression: p4067_baseline — reports baseline boxes for a fusion tree, polygon diagram, and arrow grid.
% Formula: geometry-only; no tensor identity.
\documentclass{article}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
$x$% force math fonts
\setbox0=\hbox{\tntree{(((a\,b)_x\,c)_y\,d)_e}}
\typeout{TREE ht=\the\ht0 dp=\the\dp0 axis=\the\fontdimen22\textfont2}
\setbox1=\hbox{\begin{tenkzcd}[polygon=3, radius=15mm]
  $A$ & $B$ & $C$
  \tnarrow[from=1, to=2]{f}
\end{tenkzcd}}
\typeout{POLY ht=\the\ht1 dp=\the\dp1}
\setbox2=\hbox{\begin{tenkzcd}
  A \arrow[r] & B
\end{tenkzcd}}
\typeout{GRID ht=\the\ht2 dp=\the\dp2}
x
\end{document}
