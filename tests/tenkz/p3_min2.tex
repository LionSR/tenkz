% Regression: p3_min2 — exercises grid pictures with `rows={op:operator, op:operator}` through \tn.
% Formula: geometry-only; no tensor identity.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  \begin{tenkz}[rows={op:operator, op:operator}]
    \tn{} \\
    \tn{}
  \end{tenkz}
\]
\[
  \begin{tenkz}[rows={op, op}]
    \tn{A} \\
    \tn{B}
  \end{tenkz}
\]
\end{document}
