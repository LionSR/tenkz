% Regression: p3_word — imported current-behavior fixture from the handoff corpus.
% p3_word — arXiv:1804.04964, Sec. "Injective MPS", source lines 184-195:
% the non-translationally-invariant MPS as a traced matrix word.
%
% Formula: |Psi> = sum_i tr{ A_1^{i_1} A_2^{i_2} ... A_n^{i_n} } |i_1 ... i_n>
%   ink:  one ket row (physical=up), trace closure `periodic` — the racetrack
%         return wire below the word IS the paper's trace box under the row;
%         \tndots elides the middle sites exactly where the source puts $\dots$.
% Signature: vw 0, ve 0 (traced), pu 4 (one open physical index per drawn site).
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}

MPS word (1804.04964 lines 185--194):
$|\Psi\rangle = \sum_i \operatorname{tr}\{A_1^{i_1} A_2^{i_2} \dots A_n^{i_n}\}\,|i_1 \dots i_n\rangle$
\[
  |\Psi\rangle \;=\;
  \begin{tenkz}[periodic, physical=up]
    \tn{A_1} & \tn{A_2} & \tn{A_3} & \tndots & \tn{A_n}
  \end{tenkz}
\]
\end{document}
