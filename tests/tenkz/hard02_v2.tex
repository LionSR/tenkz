% Regression: hard02_v2 — imported current-behavior fixture from the handoff corpus.
% Formula: [A_L^s]_{alpha,beta}  =  the 3-leg tensor with
% hard02 v2 — definition of the left-canonical MPS tensor (RMP AL):
%
%   [A_L^s]_{alpha,beta}  =  the 3-leg tensor with
%     west  virtual index alpha  (open stub, labelled),
%     east  virtual index beta   (open stub, labelled),
%     physical index s pointing down.
%
% Declarative rendition: ONE cell in a physical=down row.  The Phase-0.5
% boundary doctrine draws both open virtual stubs by default (boundary=open),
% and `west label` / `east label` name the boundary indices at the stub tips
% — no \tnghost padding, no free-tier coordinates.
% Noted gap: the RMP draws A_L as a right-pointing TRIANGLE (gauge-direction
% iconography); no `tri=` skin exists, so the box skin carries the label.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  [A_L^s]_{\alpha,\beta} \;=\;
  % one bra-type cell: down leg = physical index s; west/east stubs = the
  % matrix indices alpha, beta of the matrix A_L^s.
  \begin{tenkz}[physical=down, west label={$\alpha$}, east label={$\beta$}]
    \tn[box, down=$s$]{A_L}
  \end{tenkz}
\]
\end{document}
