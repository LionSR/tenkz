% Regression: t2_ared_loop — renders X |-> Gamma(X)^{s}_{s'} = Tr_D[ X A^{(s,s')} ] = sum_{alpha,beta} X_{alpha…
% T2 — RMP arXiv:2011.12127, fig. fig3-pepe_Ared: the boundary-to-bulk map
% of one G-injective MPO tensor.
% Formula:
%   X |-> Gamma(X)^{s}_{s'} = Tr_D[ X A^{(s,s')} ]
%       = sum_{alpha,beta} X_{alpha beta} A^{(s,s')}_{beta alpha},
% the physical operator obtained by inserting the boundary matrix X on the
% closed virtual (red) ring of the MPO tensor A; the physical up/down legs
% of A stay open.  The source's ": X" argument annotation is kept.
%
% Declarative reading: one operator-hued row (rows={op:operator}) whose
% `periodic` racetrack IS the virtual ring; X is \tnX, tenkz's canonical
% on-wire-matrix glyph (the source draws an open circle, same role).
% No restyle hacks: the racetrack strokes exactly what the grammar gives.
\documentclass[varwidth=19cm, border=6pt]{standalone}
\usepackage{amsmath, amssymb}
\usepackage{tenkz}
\begin{document}
\[
  % Gamma(X) = Tr_D[X A]: X on the traced virtual ring, physical legs open
  \begin{tenkz}[rows={op:operator}, periodic, tensor style=box]
    \tnX{X} & \tn{}
  \end{tenkz}
  \;\colon\; X
\]
\end{document}
